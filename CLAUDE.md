# CLAUDE.md

Two separate projects live in this repo:

- **`zabobovdol/`** — an existing project. **Do not touch it.** Never edit, move,
  delete, or run builds against it. Read it only when a task explicitly needs it
  as reference.
- **`app/`** — where all new work goes. See `app/CLAUDE.md` for working rules.

Keep sessions lean: don't explore `zabobovdol/` (large lockfile, ~70 seed
scripts) unless a task truly needs it, trust these docs instead of
re-discovering the layout, run each build/lint/test gate only once and only
when relevant, and answer in lead-with-the-outcome prose with minimal formatting.
