node_modules/
dist/
.env
*.log
.DS_Store
# Runtime state under api/logs (throttle JSON, leads, fail2ban log, rate
# counters) is server-only. Ignore everything there except the two files
# that make the directory exist and deny web access to it.
api/logs/*
!api/logs/.gitkeep
!api/logs/.htaccess
vendor/
api/smtp-local.php
