/** Trick-engine card primitives — re-exported from the shared card module. */
export {
  type Suit,
  type Card,
  SUITS,
  suitOf,
  rankOf,
  buildDeck,
  HIDDEN,
  hiddenLike,
} from "../cards.js";
