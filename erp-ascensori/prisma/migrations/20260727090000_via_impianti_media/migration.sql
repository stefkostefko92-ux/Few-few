-- Махане на `impianti_media`.
--
-- ТАБЛИЦАТА БЕШЕ КАПАН, НЕ ФУНКЦИЯ. Тя пазеше НИЗ (`url`), който човек пишеше
-- на ръка: нямаше качване, нямаше файл, нямаше проверка на типа. В интерфейса
-- стоеше втора секция, озаглавена също „Allegati", до истинската — тоест
-- техник натискаше „Aggiungi", попълваше нещо и излизаше с убеждението, че е
-- прикачил снимка от обекта. Не беше прикачил нищо.
--
-- Истинското качване е `allegati` (`/api/allegati`): тип, подушен по
-- СЪДЪРЖАНИЕ, SHA-256 на файла, път от UUID, роля и фирма проверени на входа.
-- Две врати към „прикачени файлове" значат, че едната лъже.
DROP TABLE IF EXISTS "impianti_media";

-- Таблицата излиза и от `TABELLE_CON_TENANT` (`src/lib/rls.ts`). Без това
-- проверката за покритие с политики търси политика върху несъществуваща
-- таблица, не я намира и рапортува RLS като НЕАКТИВНА — тоест махането на
-- капана би вдигнало фалшива тревога за счупена изолация.
