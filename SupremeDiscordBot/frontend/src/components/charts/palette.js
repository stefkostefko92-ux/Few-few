// frontend/src/components/charts/palette.js
// Валидирана палитра за графики (тъмна повърхност cs-surface #0d130b).
//
// НЕ сменяй тези стойности „на око" — минали са през шестте проверки на
// dataviz валидатора (OKLCH lightness лента 0.48–0.67 за тъмен режим, хрома
// под, CVD разделимост по всички двойки, нормално зрение, контраст ≥3:1):
//
//   node scripts/validate_palette.js "#5a9300,#2588c5" --mode dark \
//     --surface "#0d130b" --pairs all      → ALL CHECKS PASS
//
// Брандовото #8fe600 е ИЗВЪН лентата (L 0.837 — крещи и убива четимостта на
// съседна серия), затова живее само като UI акцент, не като цвят на серия.
// Tritan разделимостта на двойката е 6.2 (подовата лента 6–8) → задължително
// ВТОРИЧНО кодиране: легенда + директни етикети на края на всяка серия. То е
// вградено в AreaChart, не е по избор.

export const SERIES = {
  opened: { stroke: "#5a9300", fill: "rgba(90, 147, 0, 0.18)", label: "Opened" },
  closed: { stroke: "#2588c5", fill: "rgba(37, 136, 197, 0.18)", label: "Closed" },
};

// Един цвят за всички барове: разпределението е сравнение на величини, не
// идентичност — категорийна дъга там би кодирала дължината два пъти
// (анти-патърн „value-ramp on nominal categories").
export const BAR_COLOR = "#5a9300";

// Решетка/оси: плътни косъмни линии, един тон встрани от повърхността.
export const GRID = "rgba(255, 255, 255, 0.07)";
export const AXIS_TEXT = "#9a9a9a";
