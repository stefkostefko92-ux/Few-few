-- v38 — категорията „messages" излиза от подразбирането на eventLogCategories.
--
-- Тя е единствената, която чете СЪДЪРЖАНИЕ на съобщения извън тикет каналите,
-- тоест единствената, при която привилегированият MessageContent intent се
-- ползва за нещо различно от тикет транскрипта. Досега вдигането на общия ключ
-- „Server Activity Logging" я включваше мълчаливо — сървърът започваше да
-- записва чужди разговори без администраторът да е решил точно това.
--
-- ВАЖНО: сменя се САМО подразбирането за НОВИ редове. Заварените сървъри
-- запазват масива си непроменен — никой не губи функция, която вече ползва.
ALTER TABLE "servers"
  ALTER COLUMN "eventLogCategories" SET DEFAULT ARRAY['voice', 'members', 'moderation']::TEXT[];
