-- v3.1 — имейл на потребителя от Discord OAuth (scope `email`).
--
-- Цел: САМО транзакционни известия по абонамента (изтичащ пробен период,
-- провалено плащане, фактура). Основание по GDPR: чл. 6(1)(б) — необходимост
-- за изпълнение на договора; НЕ съгласие, НЕ маркетинг.
-- Nullable без default: съществуващите редове остават с NULL, докато
-- потребителят не влезе отново и не одобри новия scope. Без unique индекс —
-- Discord не гарантира уникалност на имейла между акаунти, а уникалният ключ
-- би счупил втори логин с общ имейл.

ALTER TABLE "users" ADD COLUMN "email" TEXT;
