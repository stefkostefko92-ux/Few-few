# Помагам admin — примерна конфигурация. Копирай в .env при нужда.

# Базов адрес на API-то. Празно = same-origin (зад reverse proxy в production,
# или през dev прокси-то на Vite локално).
VITE_API_BASE_URL=

# Цел на dev прокси-то (само за `npm run dev`).
VITE_DEV_API_PROXY=http://localhost:4400
