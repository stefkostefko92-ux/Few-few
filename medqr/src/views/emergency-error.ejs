<%- include('partials/head', { title: 'Грешка', user: typeof user !== 'undefined' ? user : null }) %>

<section class="form-wrap center">
  <h1>🚫 <%= message %></h1>
  <p><a class="btn btn-ghost" href="/">Към началото</a></p>
</section>

<%- include('partials/foot') %>
