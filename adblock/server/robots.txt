# Supreme AdBlock — Carbon Stealth
# One group for every crawler (search engines and AI answer engines alike):
# the site is open, the filter data is not a page. Named groups would REPLACE
# this one for that bot, which is how filters.json used to leak into some.
User-agent: *
Allow: /
Disallow: /filters.json
Disallow: /filters.json.sig

Sitemap: https://adblock.carbonstealth.eu/sitemap.xml
