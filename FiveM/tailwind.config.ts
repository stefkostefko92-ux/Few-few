import type { Config } from 'tailwindcss';

/**
 * Палитрата е ИЗМЕРЕНА от логото (`public/brand/logo.png`), не избрана на око:
 * пробите дадоха черен фон, сребърна стълбица за надписа, циан за „BG“ и
 * знамето. Не добавяй цвят, който го няма в логото — брандът е един.
 */
const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      fontFamily: {
        ui: ['ui-sans-serif', 'system-ui', 'Segoe UI', 'Roboto', 'sans-serif'],
      },
      colors: {
        // Фон: логото стои на чисто черно, интерфейсът — на почти черно, за да
        // не се слива с ръбовете на картинката.
        ink: {
          950: '#04070a',
          900: '#070c11',
          // 800 и 700 стоят като стъпала на скалата, но НЕ се ползват в кода.
          // Оставени са нарочно: палитра с дупки кара следващия да измисля
          // междинен цвят на око. Ползваш ли ги — премери пак, при тях
          // cyan-700 пада под 3:1.
          800: '#0d151c',
          700: '#16232d',
        },
        // Акцентът от буквите „BG“.
        cyan: {
          200: '#a7f0fa',
          300: '#60e0f0',
          400: '#3ccce4',
          500: '#20a8c0',
          600: '#1789a0',
          700: '#106880',
          900: '#08303c',
        },
        // Металният надпис „FIVEM“.
        silver: {
          100: '#f8f8f8',
          200: '#e8e8e8',
          300: '#d0d0d0',
          400: '#b0b0b0',
          500: '#909090',
          // 600 е ЕДИНСТВЕНОТО сиво под 4,5:1 (3,62:1 върху ink-950). Не се
          // ползва никъде и не бива да се ползва за текст — измерено, не усетено.
          600: '#686868',
        },
        // Знамето — само за акцентната лента, НИКОГА за текст: измерено, зеленото
        // дава 3,26:1, червеното 3,05:1 върху ink-950, тоест и двете падат под
        // 4,5:1. Правилото не е вкус, а следствие.
        flag: {
          white: '#f8f8f8',
          green: '#007040',
          red: '#b81810',
        },
      },
    },
  },
  plugins: [],
};

export default config;
