# Трейдъра (`treydar/`) — spot трейдинг бот за Binance

Автоматизиран **spot** трейдинг бот с **риск-мениджмънт като първо правило**. Създаден и
поддържан от агента **Трейдъра** (`.claude/agents/treydara.md`).

> ⚠️ **ВАЖНО — прочети преди да пуснеш каквото и да е.**
> Това е **софтуерен инженеринг, не инвестиционен/финансов съвет**. Автоматизираната търговия
> носи риск от **загуба на целия капитал**. Никой бот не гарантира печалба. Примерната стратегия
> (SMA crossover) е **placeholder за демонстрация**, не печеливша система. Тествай на **testnet** и
> направи **бектест**, преди да рискуваш реални пари. Използвай само средства, които можеш да
> загубиш. При търговия с чужди пари или предоставяне на „съвет“ важат лицензионни изисквания
> (в ЕС: MiFID II) — консултирай юрист.

## Какво прави (и какво НЕ прави)

**Прави:** следи един пазар (напр. `BTC/USDT`), на затворени свещи смята сигнал, при вход изчислява
размер **от риска** (не от „колко искам да вложа“), слага **stop-loss на борсата** веднага след
покупка, и спазва дневен лимит на загубата + глобален **max-drawdown kill-switch**.

**НЕ прави:** не тегли пари (не му трябват withdrawal права), не използва лост (spot), не обещава
печалба, не търгува с реални пари без изричното ти потвърждение.

## Инсталация

```bash
cd treydar
npm install
cp .env.example .env      # попълни ключове и параметри
```

## Безопасност на API ключа (задължително)

1. В Binance създай API ключ **само с „Enable Spot Trading“ + четене**.
2. **Изключи „Enable Withdrawals“.** Ботът никога не тегли.
3. Включи **IP allowlist** (restrict access) към IP-то на машината.
4. Ключовете стоят в `.env` (в `.gitignore`) — **никога** в git.

## Режими (трипосочна спирачка срещу случайна загуба)

Реални пари се движат само при **изричен** избор. Йерархия:

| `BINANCE_TESTNET` | `TRADING_LIVE` | `I_UNDERSTAND_THE_RISK` | Режим |
| --- | --- | --- | --- |
| `true` (default) | `false` (default) | — | 🟢 **DRY-RUN** — само логва намерения |
| `true` | `true` | — | 🟡 **Testnet live** — реални поръчки, фалшиви пари |
| `false` | `true` | `РАЗБИРАМ-РИСКА` | 🔴 **РЕАЛНИ ПАРИ** |

Ако поискаш реални пари без `I_UNDERSTAND_THE_RISK=РАЗБИРАМ-РИСКА`, ботът **отказва да стартира**.
Това е нарочно.

## Препоръчан път (не прескачай стъпки)

```bash
# 1) Бектест (публични данни, без ключове). Гледай walk-forward: бие ли B&H устойчиво през фолдовете?
npm run backtest                 # или: node src/backtest.js ETH/USDT 4h

# 2) Тестове на риск логиката
npm test

# 3) Dry-run срещу реални цени, но БЕЗ поръчки
npm run dry

# 4) Testnet live (ключове от https://testnet.binance.vision, BINANCE_TESTNET=true, TRADING_LIVE=true)
npm start

# 5) Чак след като си доволен: реален Binance с малка сума и всичките три флага.
```

## Статичен одит (агентските инструменти)

```bash
npm run lint    # trader-lint.mjs + backtest-check.mjs върху src/
```

## Структура

```
index.js            вход: .env → конфиг → старт
src/config.js       валидация + gating на "живо" (сърце на безопасността)
src/exchange.js     ccxt Binance spot фабрика (+ testnet)
src/marketdata.js   само ЗАТВОРЕНИ свещи (без look-ahead) + капитал
src/indicators.js   SMA/EMA/ATR/RSI (чисти, без look-ahead)
src/strategy.js     стратегии: "trend" (EMA cross + тренд филтър + RSI + ATR стоп) или "sma"
src/risk.js         размер от риска, стоп, дневен лимит, max-drawdown kill-switch (чисти функции)
src/execute.js      идемпотентни поръчки (clientOrderId + reconcile), precision, стоп на борсата
src/state.js        трайно състояние между рестарти (equity връх, дневен старт, kill)
src/bot.js          главният цикъл: данни → сигнал → риск → изпълнение (+ trailing stop)
src/metrics.js      Sharpe/Sortino/Calmar/profit factor/expectancy/max DD/exposure
src/backtest.js     ПРОФ. бектест: walk-forward фолдове + buy&hold бенчмарк + robustness grid
src/engine.js       споделен симулационен двигател (backtest + portfolio)
src/journal.js      трейд дневник (R-multiples) → data/trades.jsonl
src/coach.js        тренер: expectancy, ¼-Kelly, маркира повтарящи се грешки
src/review.js       `npm run review` — коучинг доклад от дневника (учи от грешки)
src/portfolio.js    корелация, групиране, портфейлни лимити (Dalio „Holy Grail“)
src/portfolio-backtest.js  `npm run portfolio` — диверсификационен ефект на кошница
docs/principles.md  дестилат от великите трейдъри (Turtles/PTJ/Soros/Thorp/Kovner/Dalio…)
test/               unit тестове (risk, indicators, strategy, coach, portfolio, extras)
data/               одит лог + състояние + дневник (в .gitignore)
```

## Още професионални предпазители

- **ADX режим-филтър** (`ADX_MIN`, напр. 22) — търгувай само в достатъчно силен тренд; извън тренд
  (страничен chop) не влиза. Пази от най-лошия пазар за тренд-стратегия.
- **Честотни спирачки** (психология → правила): `MAX_TRADES_PER_DAY` (срещу over-trading) и
  `COOLDOWN_MINUTES` след **губеща** сделка (срещу revenge trading). Наложени в кода, не с воля.
- **Monte Carlo risk-of-ruin** (в `npm run backtest`): bootstrap на сделките оценява P(просадка ≥50%)
  при различен риск/сделка — показва как по-големият риск води до разоряване. „Оцеляването е първо.“
- **Портфейл `npm run portfolio`** (Dalio „Holy Grail“): пуска кошница символи, смята **корелацията**
  на стратегийните им потоци, **групира** корелираните (напр. BTC/ETH = един залог) и показва как
  комбинирането на **некорелирани** потоци сваля волатилността/просадката.

## Мулти-символен (портфейлен) режим

`SYMBOLS` с **≥2 символа** (обща quote валута) превключва бота в портфейлен режим (`src/multibot.js`):

- Всеки символ минава през **същите предпазители** като едно-символния: риск-гейтове (дневен лимит,
  max-drawdown kill-switch), честотни спирачки, ATR стоп на борсата, trailing, трейд дневник.
- Отгоре са **портфейлните лимити** (проверявани преди всеки вход): `MAX_CONCURRENT` (едновременни
  позиции), `MAX_PORTFOLIO_RISK_PCT` (таван на сумарния отворен риск) и `MAX_GROUP_RISK_PCT` — таван на
  риска в една **корелирана група**: корелацията се смята на живо от затворените свещи и символи с
  |corr| ≥ `CORR_THRESHOLD` се третират като **един залог** (напр. BTC+ETH), не два.
- В **dry-run** позициите са paper (симулиран стоп, натрупан paper PnL) — целият портфейлен цикъл се
  репетира без поръчки; в **live** борсата остава източник на истината за позициите.

Преди мулти-символен live: първо `npm run portfolio` (има ли изобщо диверсификационен ефект в твоята
кошница?), после dry-run, после testnet. Не прескачай стъпки.

## Учене от грешките (`npm run review`)

Ботът записва всяка затворена сделка в **R-multiples** (`data/trades.jsonl`). `npm run review` смята
**expectancy** `E[R]=p·avgWinR+(1−p)·avgLossR`, profit factor, стоп-out %, серии загуби, и **маркира
повтарящи се грешки**: отрицателна expectancy, лоша асиметрия („режеш печалби/държиш загуби"), твърде
стегнат стоп, дълга серия загуби (сменен режим), win-rate капан. Предлага и **¼-Kelly риск%** от
реалната статистика (дробен Kelly срещу variance drag; 0 при <30 сделки или без едж). Пусни го редовно —
това е механизмът, по който системата се учи от собствените си резултати.

Философията зад всичко това (проучена от известни трейдъри, с източници) е в **`docs/principles.md`**.

## Стратегии

По подразбиране **`STRATEGY=trend`**: влиза само по посока на дългия тренд (цена над `EMA_TREND`),
на `EMA_FAST`/`EMA_SLOW` cross нагоре, ако не е прегрят (`RSI < RSI_OVERBOUGHT`); стопът е **ATR-базиран**
(`вход − ATR × ATR_MULT`) — адаптира се към волатилността, вместо фиксиран %. `USE_TRAILING=true`
качва стопа нагоре при движение в твоя полза. Има и прост `STRATEGY=sma` (crossover) за сравнение.

⚠ Нито една от тях не е гаранция за печалба. Смисълът на бектеста е да покаже дали изобщо си струва.

## Как да четеш бектеста (`npm run backtest`)

- **Walk-forward фолдове** — 4 последователни out-of-sample прозореца. Търсиш **устойчивост**:
  печели ли в повечето фолда, или само в един (късмет)? Ред `бие B&H в X/4 фолда` е ключов.
- **Buy & Hold (B&H)** — ако не биеш простото държане устойчиво, стратегията **не струва** за реални
  пари. `✅/❌` до всеки фолд показва това.
- **Метрики** — Sharpe/Sortino (доходност спрямо риск), max DD (най-лошото пропадане), profit factor
  (>1.5 е прилично), win% и expectancy, exposure (колко време си в пазара).
- **Robustness grid** — вариране на `atrMult × emaFast`. **Тесен и положителен разлив** = устойчиво;
  голям разлив min→max = крехко/overfit (не вярвай на един „щастлив“ набор параметри).

## Как да замениш/донастроиш стратегията

Пипаш `src/strategy.js`: `signalAt(ctx, i)` връща `'long' | 'exit' | null` за **затворената** свещ `i`;
`stopDistance(ctx, i, entry)` дава разстоянието до стопа. Дисциплина: никакво четене на бъдещи барове
(има тест за това); сигналът се изпълнява на отваряне на следващия бар. След всяка промяна →
`npm run backtest` и гледай walk-forward + robustness, преди testnet/live.

---

**Дисклеймър:** Не е инвестиционен, финансов или данъчен съвет. Търговията носи риск от загуба на
целия капитал. Нищо тук не гарантира печалба.
