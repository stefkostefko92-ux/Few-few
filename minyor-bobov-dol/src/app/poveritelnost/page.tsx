import type { Metadata } from "next";
import { buildMetadata, webPageLd } from "@/lib/seo";
import { SITE } from "@/lib/site";
import { PageHero, Prose } from "@/components/ui";
import { JsonLd } from "@/components/JsonLd";
import { markdownToHtml } from "@/lib/markdown";

export const metadata: Metadata = buildMetadata({
  title: "Политика за поверителност (GDPR)",
  description:
    "Как ФК „Миньор“ Бобов дол обработва и защитава личните данни на посетителите на сайта.",
  path: "/poveritelnost",
});

const CONTENT = `
Тази политика обяснява как се обработват личните данни на посетителите на
официалния сайт на ФК „Миньор“ Бобов дол, в съответствие с Регламент (ЕС)
2016/679 (GDPR) и Закона за защита на личните данни.

## Кой обработва данните

Сайтът е изработен и се поддържа от **Carbon Stealth VCC** в полза на клуба.
За въпроси по защитата на данните: **${SITE.contact.email}**.

## Какви данни събираме

- **Форма за контакти:** име, имейл и текст на съобщението — за да отговорим на
  запитването ви. Не ги използваме за други цели и не ги предоставяме на трети
  страни с маркетингова цел.
- **Технически данни:** при работа на сайта се обработват минимални технически
  данни (напр. IP адрес в логовете на сървъра) за сигурност и наличност.

Не използваме рекламно проследяване и не профилираме посетителите.

## Основание и срок на съхранение

Обработваме данните от формата въз основа на вашето съгласие и легитимния
интерес да отговорим. Съхраняваме съобщенията само толкова, колкото е нужно за
обработката на запитването, след което се изтриват.

## Вашите права

Имате право на достъп, коригиране, изтриване, ограничаване и възражение срещу
обработката, както и право на жалба до Комисията за защита на личните данни
(КЗЛД). За упражняване на правата пишете на **${SITE.contact.email}**.

## Бисквитки

Използваме само технически необходими бисквитки. Подробности — в страница
[Бисквитки](/biskvitki).
`;

export default function PrivacyPage() {
  return (
    <>
      <PageHero
        title="Политика за поверителност"
        eyebrow="GDPR"
        crumbs={[{ name: "Поверителност", path: "/poveritelnost" }]}
      />
      <JsonLd
        data={webPageLd({
          name: "Политика за поверителност",
          path: "/poveritelnost",
          lastReviewed: "2026-06-01",
        })}
      />
      <div className="container-content max-w-3xl py-10">
        <Prose html={markdownToHtml(CONTENT)} />
      </div>
    </>
  );
}
