# Пътна карта v2.0 на агентите

Общата тема: всеки агент прескача от **„съветник“ → „инструментиран изпълнител с
обратна връзка“** — пуска реални инструменти, проверява с данни и получава скрипт(ове)
в `tools/<area>/` + CI гейт. Тайните/ключовете остават на VPS-а (mode 600), никога в архива.

Статус: ✅ реализирано сега · 🟡 планирано (нужни услуги/креденшъли). Усилие: S/M/L.

## ⚖️ Правният Разбирач
- ✅ Runtime consent скенер (`tools/legal/consent-scan.mjs`, Playwright) — тракинг преди съгласие. `M`
- ✅ a11y гейт (`tools/legal/a11y.mjs`, axe-core, WCAG 2.1 AA). `S`
- 🟡 RoPA (чл. 30) + DPIA генератор от `data-map.yaml`; слоести политики; TCF v2.3 / Consent Mode v2 валидатор. `L`/`M`

## 🛠️ Кодаджията
- ✅ SAST/SCA оркестратор (`tools/code/scan.sh`: Semgrep + osv-scanner + gitleaks + SBOM, репо-правила). `M`
- ✅ Адъюдикация (reachability/feasibility) — маха 72–96% FP. `M`
- 🟡 Mutation testing на diff (StrykerJS); auto-fix PR с inline коментари. `L`

## 🎮 Геймъра
- ✅ Lint (`tools/fivem/.luacheckrc` + `selene.toml`/`fivem.yml`) + CI (`ci.example.yml`). `S`
- ✅ Тест-поток с мокнати natives (busted) + профилиране/сигурност (README). `M`
- 🟡 Event-fuzz harness; scaffold + semantic-release. `L`

## 🔎 SEO
- ✅ CWV (`tools/seo/cwv.mjs`, PSI/CrUX), JSON-LD валидатор, AI-bot лог анализ. `M`
- 🟡 Search Console API ingest; Lighthouse CI budget гейт. `M`

## 🌍 Преводач
- ✅ Паритет (`check-parity.mjs`) + псевдолокализация (`pseudo.mjs`) + глосар enforcement. `S`
- ✅ +WebFetch/WebSearch за проверка на клинични термини. `S`
- 🟡 COMET/CometKiwi + back-translation adequacy гейт; i18next-cli екстракция; TMX памет. `L`

## 🌱 Сийдъра
- ✅ Zod-валидиран сийд с provenance (`tools/seed/zod-factory.example.ts`) + dedup (`check-dups.mjs`). `S`
- 🟡 Open-data pipeline (data.egov.bg / НЗИС / data.europa.eu) raw→transform; integrity/FK гейт; Prisma 7. `M`

## 🖥️ VPS-аджията
- ✅ IaC скелет (`tools/vps/ansible/site.yml`); backup+test-restore (`backup-verify.sh`); мониторинг (Beszel + Uptime Kuma). `S`/`M`
- ✅ sha256 целост (в `autodeploy.sh`); sops+age документиран. `S`
- 🟡 Blue/green превключване на upstream; cosign verify гейт. `M`

## 🧩 3D Maniac
- ✅ RANSAC сегментация (`ransac_segment.py`); параметрична форма (`generate_mold.py`); deviation (`clean_and_validate.py`). `S`/`M`
- 🟡 cadrille/CAD-Recode scan→CadQuery интеграция; PyNite FEA скрининг. `L`

## 🎬 Социалджията
- ✅ Trend-sensing (`trends.py`); клип pipeline (`clip.sh`); C2PA подпис (`c2pa-sign.sh`); публикуващ поток (`publish.md`). `M`
- 🟡 Официални публикуващи API (TikTok draft/IG/YouTube); HeyGen/ElevenLabs faceless серии; analytics итерация. `L`

---

### Принципи за v2.0 (важат за всички)
- Инструментите намират кандидати; **агентът доказва или изхвърля** (нула недоказани).
- Зелен гейт ≠ съответствие (axe ~57%; AI scan→CAD = чернова; AI-citation = тренд).
- Безопасно-критичното (медицина, структурни части, право) — **човек в цикъла**, без авто-merge.
- Тайни/ключове/логове на сървъра (mode 600); скрейпъри/ToS нарушения = забранени.

## 📈 Трейдъра
- ✅ Статичен линт за трейдинг код (`tools/trading/trader-lint.mjs`: clientOrderId, сляп ретрай, float
  пари, липсващ стоп/kill-switch, WS reconnect, look-ahead) + `backtest-check.mjs`. `M`
- ✅ Реален бот-полигон `treydar/` (тройна спирачка, риск-first, walk-forward + Monte Carlo, R-дневник
  + тренер `npm run review`, портфейлен режим) + CI (Node 20/22). `L`
- 🟡 Testnet e2e (изисква разрешен регион/IP — Binance 451 в CI); Discord известия за fills/kill-switch
  (с Дискорджията); контрактни тестове с фикстури от борсови отговори (partial fill, 429, disconnect). `M`

## 📣 Рекламчика
- ✅ Статичен линт за рекламен код (`tools/ads/ads-lint.mjs`: hardcoded токени, ACTIVE-at-create,
  teen profiling, липсващи `special_ad_categories`/`dsa_payor`/CAPI `event_id`, micros, tight retry). `M`
- ✅ Реален продукт-полигон `SupremeAdManager/` (guard.js предпазители, PAUSED-first, dry-run симулатор,
  двигател на правила с cooldown/min-spend, одитна следа, 13 smoke теста). `L`
- 🟡 Реален OAuth flow в UI (Google authorization-code + Meta FB Login for Business); insights графики;
  Consent Mode v2 сигнали при conversion upload; creative upload (adimages/advideos + YouTubeVideoUploadService);
  A/B експерименти през native APIs. `L`
- 🟡 От prior art (2026-07-12): policy/disapproval мониторинг в scheduler-а (ads-policy-monitor
  pattern — тихият фал); Meta native automated rules (`adrules_library`) като backstop при паднал
  scheduler (обхват непотвърден); connector lifecycle hooks + GAQL-като-файлове (Opteo/gaarf);
  аномалии по „същия ден от седмицата“ (Account Anomaly Detector). `M-L`
