# Памет на агента „VPS-аджията" (v6.0 — самообучение)

Трайно файлово знание между извикванията (Claude Code субагентите са stateless).
Цикълът е **наложен от hooks** (виж `_memory/PROTOCOL.md`): при старт `SubagentStart`
инжектира „Проверени поуки"; накрая `SubagentStop` добавя новия ```learn блок
(verified → тук; друго → Карантина); `tools/memory/curate.mjs` дедупира и пази от дрейф.
**Закон:** само проверено става факт; източник или нищо; противоречие → стоп (човек решава).

## Проверени поуки (verified)
- **2026-07-28:** Let's Encrypt спря имейлите за изтичащ сертификат на 04.06.2025 (приватност + разходи) — няма вече човешка предпазна мрежа при счупен renew. Следенето на срока на сертификата (Uptime Kuma / външен монитор) става задължителен контрол, не опция. _("TLS мониторинг на всички домейни зад Let's Encrypt"; verified; "https://letsencrypt.org/2025/01/22/ending-expiration-emails/")_
- **2026-07-28:** prisma migrate deploy взима advisory lock с 10-секунден НЕКОНФИГУРУЕМ таймаут → при blue/green два едновременно стартирали екземпляра единият пада с таймаут. Миграцията трябва да е отделна стъпка в деплоя, не side-effect на старта на приложението. Плюс expand-contract ръбове: CREATE INDEX CONCURRENTLY не може в транзакция и при провал оставя INVALID индекс, който продължава да струва при всеки UPDATE (drop/REINDEX CONCURRENTLY); ALTER TABLE взима ACCESS EXCLUSIVE освен изрично отбелязаните форми; ADD COLUMN с неволатилен DEFAULT не пренаписва таблицата, но волатилен DEFAULT/stored generated колона пренаписват цялата таблица и индексите. _("нулев престой при схемни миграции (expand-contract) за Prisma+Postgres проектите"; verified; "https://www.prisma.io/docs/orm/prisma-migrate/workflows/development-and-production ; https://www.postgresql.org/docs/18/sql-createindex.html ; https://www.postgresql.org/docs/18/sql-altertable.html"; re-verify: 2027-06-01)_
- **2026-07-28:** Официалният postgres Docker образ: скриптовете в /docker-entrypoint-initdb.d се пускат САМО при празна data директория; ако някой падне, ентрипойнтът излиза и при рестарт с вече инициализирана директория НЕ ги продължава → полу-сийдната база, която изглежда здрава. По време на инициализацията временният демон слуша САМО на Unix сокет (docker-library/postgres#440) → health check през сокет може да мине рано; пробата трябва да е TCP (pg_isready -h 127.0.0.1). _("zabobovdol/Supreme compose стек — сийдване само първия път и честен healthcheck на db"; verified; "https://raw.githubusercontent.com/docker-library/docs/master/postgres/content.md (редове 121 и 138, живо 2026-07-28)"; re-verify: 2027-06-01)_
- **2026-07-28:** PostgreSQL 17 въведе нативен инкрементален физически бекъп: pg_basebackup --incremental + новото приложение pg_combinebackup, захранени от WAL summarization файлове (server var summarize_wal). PG18 добави pg_combinebackup -k/--link (hard link — НЕ се ползва, ако бекъпите ще се ползват независимо) и pg_verifybackup за tar-формат. Това е алтернатива на pgBackRest за малък VPS, но веригата base+инкременти трябва да е цяла. _("бекъп стратегия за Postgres ≥17 на един VPS (tools/vps/backup-verify.sh)"; verified; "https://www.postgresql.org/docs/release/17.0/ ; https://www.postgresql.org/docs/release/18.0/"; re-verify: 2027-06-01)_
- **2026-07-28:** pg_dump НЕ изнася оптимизаторска статистика — '--no-statistics' е ДЕФОЛТЪТ (PG18 добави --statistics/--statistics-only/--no-data/--no-schema). Следствие: възстановена от dump база тръгва без статистика и плановете са катастрофални под товар, докато не мине ANALYZE/vacuumdb --analyze-in-stages. pg_upgrade в PG18 вече запазва статистиката, но РАЗШИРЕНАТА статистика не се пренася (има и --no-statistics за изключване). _("restore/миграция на Postgres (zabobovdol, Supreme, linketto) — задължителен ANALYZE след restore"; verified; "https://www.postgresql.org/docs/18/app-pgdump.html ; https://www.postgresql.org/docs/release/18.0/"; re-verify: 2027-06-01)_
- **2026-07-28:** Dockerfile shell-форма на CMD/ENTRYPOINT увива командата в /bin/sh -c, който НЕ препраща сигнали → приложението не е PID 1 и не получава SIGTERM от 'docker stop' → всеки стоп е SIGKILL след stop_grace_period (по подразбиране 10s), без graceful drain. Exec-форма (JSON масив) е задължителна. От Node 19.0.0 server.close() сам затваря idle keep-alive връзките (по-рано трябваше closeIdleConnections(), добавен в 18.2.0). _("контейнеризирани Node приложения — graceful shutdown при деплой/blue-green"; verified; "https://docs.docker.com/reference/dockerfile/ ; https://docs.docker.com/reference/compose-file/services/ ; https://nodejs.org/api/http.html"; re-verify: 2027-06-01)_
- **2026-07-28:** Docker HEALTHCHECK: провалите по време на --start-period не се броят, НО първият успех прекратява старт-периода и оттам всички поредни провали вече броят към --retries. Прекалено ранна (TCP/порт) проба изяжда гратиса. Дефолти: interval 30s, timeout 30s, retries 3, start-period 0s, start-interval 5s (иска Engine ≥25.0); пробата се убива със SIGKILL при timeout. _("Docker/Compose healthcheck дизайн — zabobovdol, SupremeDiscordBot, eternaltouch"; verified; "https://docs.docker.com/reference/dockerfile/ (раздел HEALTHCHECK, dateModified 2026-06-18)"; re-verify: 2027-01-15)_
- **2026-07-28:** nginx proxy_buffering е 'on' по подразбиране (буферира отговора) и убива SSE/streaming, а proxy_read_timeout е 60s (къса idle SSE/WS). Приложението може да изключи буферирането САМО за своя отговор с заглавка 'X-Accel-Buffering: no' — без промяна в nginx конфига (може да се игнорира с proxy_ignore_headers). _("SSE/streaming/long-poll ендпойнти зад nginx"; verified; "https://nginx.org/en/docs/http/ngx_http_proxy_module.html"; re-verify: 2027-06-01)_
- **2026-07-28:** nginx worker_shutdown_timeout НЯМА стойност по подразбиране (Default: —) → след 'nginx -s reload' старите работници могат да живеят неограничено и да продължат да обслужват СТАРИЯ upstream (дълги SSE/WebSocket/keepalive). Blue/green upstream суичът не е атомарен без изрично зададен worker_shutdown_timeout. _("blue/green превключване през nginx reload (tools/vps/blue-green.sh) — дренаж на старата версия"; verified; "https://nginx.org/en/docs/ngx_core_module.html"; re-verify: 2027-01-15)_
- **2026-07-28:** nginx 1.29.7 (24.03.2026) смени дефолтите: proxy_http_version вече е 1.1, upstream keepalive е ВКЛЮЧЕН по подразбиране (keepalive 32 local) и заглавката 'Connection' повече НЕ се праща към бекенда. Класическите 'proxy_http_version 1.1; proxy_set_header Connection \"\";' са излишни на ≥1.29.7; WebSocket пак иска изричен Upgrade + Connection: upgrade. _("nginx reverse proxy пред Node/Next (medqr, zabobovdol, ospedali) — конфиг и очаквано поведение при blue/green дрениране"; verified; "https://nginx.org/en/docs/http/ngx_http_proxy_module.html ; https://nginx.org/en/docs/http/ngx_http_upstream_module.html ; https://nginx.org/en/CHANGES (запис за 1.29.7, живо 2026-07-28)"; re-verify: 2026-11-01)_
- **2026-07-28:** Type=notify изисква приложението да прати READY=1 през sd_notify; Node няма вграден sd_notify → без него стартът виси до DefaultTimeoutStartSec=90s и unit-ът се обявява за failed. NotifyAccess се форсира на 'main', ако липсва. notify-reload иска RELOADING=1 + MONOTONIC_USEC при всеки reload. _("Node сервизи под systemd — не слагай Type=notify без реална sd_notify имплементация"; verified; "https://www.freedesktop.org/software/systemd/man/latest/systemd.service.html ; https://www.freedesktop.org/software/systemd/man/latest/systemd-system.conf.html"; re-verify: 2027-01-15)_
- **2026-07-28:** RestartSec= по подразбиране е 100ms, а стартовият rate limit е DefaultStartLimitIntervalSec=10s / DefaultStartLimitBurst=5 → мигновен crash-loop изчерпва квотата за под 1s и systemd СПИРА да рестартира unit-а (нужен systemctl reset-failed). RestartSec=3 (medqr.service:13) дава 4 старта/10s — под прага. За дълги инциденти: RestartSteps= + RestartMaxDelaySec= (systemd ≥254) за експоненциален backoff. _("systemd unit-и с Restart= — оразмеряване на RestartSec и диагностика на 'start request repeated too quickly'"; verified; "https://www.freedesktop.org/software/systemd/man/latest/systemd.service.html ; https://www.freedesktop.org/software/systemd/man/latest/systemd.unit.html ; https://www.freedesktop.org/software/systemd/man/latest/systemd-system.conf.html ; /home/user/Few-few/medqr/deploy/systemd/medqr.service:12-13"; re-verify: 2027-01-15)_
- **2026-07-28:** systemd Type=simple (нашият medqr.service:7) прави `systemctl start/restart` да ВРЪЩА УСПЕХ дори ако бинарът липсва или User= не съществува — деплой изглежда зелен, услугата е мъртва. Type=exec чака execve() и връща провал; Type=exec се подразбира АВТОМАТИЧНО, ако unit-ът ползва credentials (LoadCredential=). _("всеки Node/systemd сервиз (medqr, vizitka, ospedali, mastilko) — избор на Type= и стойност на health check след restart"; verified; "https://www.freedesktop.org/software/systemd/man/latest/systemd.service.html ; /home/user/Few-few/medqr/deploy/systemd/medqr.service:7"; re-verify: 2027-01-15)_
- **2026-07-27:** `StartLimitIntervalSec`/`StartLimitBurst` живеят в секция **[Unit]**, НЕ в [Service] — сложени в [Service] systemd ги ИГНОРИРА мълчаливо („Unknown key name … ignoring"), тоест защитата от рестарт-цикъл я няма, а unit файлът изглежда сякаш я има. `systemd-analyze verify <unit>` го хваща за секунда и работи дори когато systemd НЕ е PID 1 — евтин гейт за всеки unit файл в CI/агентска среда. _(systemd; verified; "изпълнено: systemd-analyze verify vps-dashboard/deploy/vps-dashboard.service → `:24: Unknown key name StartLimitIntervalSec in section Service, ignoring`; след преместване в [Unit] съобщението изчезва")_
- **2026-07-27:** Деплой скрипт, който преинсталира unit файла на услуга (install -m 644 …), заличава всяка локална персонализация, направена със sed при инсталацията. Локалните разлики (ExecStart с реалния път до node, WorkingDirectory, ReadWritePaths) трябва да живеят в drop-in (/etc/systemd/system/<unit>.d/10-local.conf) — с празен ExecStart=/ReadWritePaths= за нулиране на наследеното. _(deploy; verified; "Few-few/deploy/autodeploy.sh:603 срещу vps-dashboard/deploy/install.sh:169-174; същият патърн вече е доктрина в vps-dashboard/CLAUDE.md („Лимитите са drop-in, не редакция на unit файла\")")_
- **2026-07-27:** systemd-analyze verify работи и когато systemd не е PID 1 (is-system-running=offline) — валидира синтаксиса на unit файл и проверява дали ExecStart сочи към съществуващ изпълним файл. Евтин гейт за unit-и в CI/агентска среда без systemd. _(systemd; verified; "изпълнено локално: systemd 255 (255.4-1ubuntu8.14), `systemd-analyze verify vps-dashboard.service` → „Command /usr/bin/node is not executable: No such file or directory\"")_
- **2026-07-27:** sed с # за разделител се чупи от път, който съдържа # (unknown option to `s'), а & в ЗАМЕСТВАЩИЯ низ се разгъва до целия мач и произвежда мълчаливо повреден ред. Валидирай пътищата (^[A-Za-z0-9._/-]+$) преди да ги вкараш в sed. _(bash; verified; "изпълнено локално: NODE_BIN='/opt/no&de/bin/node' → „ExecStart=/opt/noExecStart=/usr/bin/node server.jsde/bin/node server.js\"; NODE_BIN с # → sed: unknown option to `s'")_
- **2026-07-27:** Под `set -euo pipefail` неуспешен `grep -q X && sed …` НЕ прекратява скрипта — провалът на неfinalната команда в &&-списък е освободен от set -e. Следствие: такъв ред е ТИХ no-op, а не гейт. Ако конфигурацията ЗАВИСИ от sed-а, пиши го с if/else и die. _(bash; verified; "изпълнено локално: bash 5.2.21, `set -euo pipefail; grep -q NOPE /etc/hostname && echo found; echo продължавам` → изход 0")_
- **2026-07-27:** В официалния postgres Docker образ суперпотребителят се казва $POSTGRES_USER, не „postgres\" — entrypoint-ът прави `initdb --username=\"$POSTGRES_USER\"`. Затова `pg_dump -U postgres` / `psql -U postgres` падат с „role postgres does not exist\" на всеки стек с личен POSTGRES_USER. Чети го от контейнера: `docker exec $c printenv POSTGRES_USER`. _(backup; verified; "docker-library/postgres docker-entrypoint.sh:92 и :207; zabobovdol/docker-compose.yml:17 (zabobovdol), SupremeDiscordBot/docker-compose.yml:25 (bot), eternaltouch/docker-compose.yml:8 (eternaltouch)")_
- **2026-07-27:** Път в ReadWritePaths=, който НЕ съществува и НЕ е с префикс „-\", проваля старта на unit-а (226/NAMESPACE). Кодът: `if (r == -ENOENT && m->ignore) return 0;` — без ignore се връща грешка. Винаги пиши `-/път` за папки, които може да ги няма при първа инсталация. _(systemd; verified; "systemd v255 namespace.c:1543; freedesktop.org systemd.exec(5): „Paths in ReadWritePaths= … may be prefixed with '-', in which case they will be ignored when they do not exist.\"")_
- **2026-07-27:** ReadWritePaths=/root НЕ отменя ProtectHome=read-only за СЪЩИЯ път. При равни пътища systemd сортира по MountMode (MOUNT_READ_ONLY е преди MOUNT_READ_WRITE в enum-а „ordered by priority\") и drop_duplicates() пази ПЪРВИЯ — по-рестриктивния — като OR-ва read_only флага. Влагането работи само за ПОДдиректории. За писане в /root: ProtectHome=tmpfs + BindPaths=/root, или ProtectHome=false. _(systemd; verified; "systemd v255 src/core/namespace.c:172-176 (protect_home_read_only_table), :713-732 (mount_path_compare), :755-781 (drop_duplicates), :53-79 (MountMode enum), :2194 (append_access_mounts); свалено от raw.githubusercontent.com/systemd/systemd/v255")_
- **2026-07-24:** Postgres docs изрично: pg_dump 'по принцип не е правилният избор за редовни бекъпи на прод база' (логическа снимка, без PITR/incremental) — за прод RPO използвай физически базов бекъп + WAL архивиране (pg_basebackup/pgBackRest), а pg_dump пази за миграция/export и logical dump за reporting. _("стратегия бекъп прод vs миграция"; verified; "https://www.postgresql.org/docs/current/app-pgdump.html")_
- **2026-07-24:** PostgreSQL текуща серия = 18 (latest 18.4, EOL 2030-11-14). За нов zabobovdol/supreme клъстер PG18 е актуалният избор; PG16 (от CLAUDE.md таблицата) остава поддържан но не е най-новият. _("избор на Postgres мажорна версия"; verified; "https://endoflife.date/api/postgresql.json")_
- **2026-07-24:** journald Storage=auto (Ubuntu default) се държи като 'persistent' САМО ако вече съществува /var/log/journal (иначе volatile в RAM) → за траен журнал mkdir /var/log/journal + systemd-tmpfiles/restart. SystemMaxUse по подр. 10% от FS капнато на 4G, SystemKeepFree 15%; при почти пълен диск journald пази KeepFree за сметка на MaxUse. _("journald персистентност/дисково място"; verified; "https://www.freedesktop.org/software/systemd/man/latest/journald.conf.html")_
- **2026-07-24:** acme.sh е ACME клиент чист Shell (нула Python зависимости) и по подразбиране CA-то му е ZeroSSL (не Let's Encrypt — превключва се с --server letsencrypt); авто-подновяване през daily cron 'acme.sh --cron'. За минимална повърхност/без Python на хоста е алтернатива на certbot; поддържа 150+ DNS API за wildcard (DNS-01). _("избор на ACME клиент vs certbot"; verified; "https://github.com/acmesh-official/acme.sh")_
- **2026-07-24:** certbot от 2.0.0 генерира по подразбиране ECDSA secp256r1 (P-256) частни ключове (не RSA); --key-type/--elliptic-curve контролират. --deploy-hook се пуска САМО при реално подновяване (не при no-op renew), а изходният код на 'certbot renew' е 0 и при успех, и при 'нищо за подновяване' → верифицирай успеха през deploy-hook, не през exit code. --reuse-key пази същия ключ при renew (по подр. НЕ). _("certbot ключове и renew hooks"; verified; "https://eff-certbot.readthedocs.io/en/stable/using.html")_
- **2026-07-24:** fail2ban systemd backend: journalmatch (от filter-а) е ЗАДЪЛЖИТЕЛЕН; journalflags по подр. 1 (LOCAL_ONLY), =4 (SYSTEM_ONLY) изключва user session файлове. По подразбиране чете САМО актуалния (не ротирания) журнал → findtime сяга максимум до последната ротация; за търсене в целия журнал задай rotated=on. allowipv6 по подр. 'auto'. backend=auto превключва към systemd ако няма logpath файлове. _("fail2ban с journald на Ubuntu 24.04"; verified; "https://github.com/fail2ban/fail2ban/blob/master/man/jail.conf.5")_
- **2026-07-24:** oomd.conf по подразбиране: DefaultMemoryPressureLimit=60% (над този дял време под pressure за по-дълго от DefaultMemoryPressureDurationSec=30s → action), SwapUsedLimit=90%. Поне едно от MemoryPressureAbove=/SwapUsageMax= е задължително в ruleset (иначе безусловно се тригва). kill режими: kill-by-memory-pressure (най-висок reclaim rate) или kill-by-swap (най-много swap). _("systemd-oomd конфигурация"; verified; "https://man.archlinux.org/man/oomd.conf.5")_
- **2026-07-24:** systemd-oomd ползва PSI (pressure stall info) и действа само на cgroups с ManagedOOMSwap=/ManagedOOMMemoryPressure= set to 'kill' (само leaf cgroups). ИЗИСКВА включен swap за да работи добре — без swap системата livelock-ва по-бързо и swap-базираните действия се игнорират. По-добра защита от класическия kernel OOM killer, който удря по RSS. _("OOM защита на малък VPS"; verified; "https://man.archlinux.org/man/systemd-oomd.service.8")_
- **2026-07-24:** systemd ProcSubset=pid (по подр. 'all') прави невидими в /proc всички файлове/директории, несвързани с process management (напр. /proc/sys, /proc/kcore, /proc/kallsyms остатъци) чрез subset=pid mount опцията на procfs. Допълва ProtectProc= (hidepid). Може да чупи app, който чете /proc/sys — тествай. _("systemd /proc изолация"; verified; "https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html")_
- **2026-07-24:** systemd SystemCallArchitectures=native ограничава unit-а само до native syscalls и блокира вторични ABI (напр. x86 на x86-64) — без него seccomp SystemCallFilter може да се заобиколи през вторичния ABI. Каноничен избор за втвърден unit; допълва SystemCallFilter, не го заменя. _("systemd seccomp втвърдяване (medqr/ospedali)"; verified; "https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html")_
- **2026-07-24:** systemd LoadCredentialEncrypted=ID:PATH прави тайните достъпни като файл в $CREDENTIALS_DIRECTORY — read-only, backed от НЕСУАПВАЩА памет, достъпен само за User=/DynamicUser= на unit-а (+root), декриптиран/автентикиран (systemd-creds, ключ от TPM2 или /var/lib/systemd/credential.secret). По-силен модел от EnvironmentFile (env течe в /proc и се наследява от child-ове). Кандидат за medqr вместо EnvironmentFile=/etc/medqr/medqr.env. _("systemd тайни за medqr/Node услуги"; verified; "https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html")_
- **2026-07-24:** nginx add_header КАПАН: директивите се наследяват от предходното конфиг ниво САМО ако на текущото ниво няма НИТО ЕДИН add_header. Т.е. един add_header в location блок изтрива ВСИЧКИ add_header от server ниво (HSTS/CSP/X-Content-Type-Options тихо изчезват за този location). Фикс: повтори всички header-и в location, или дефинирай ги на едно ниво. 'always' праща header-а и при 4xx/5xx отговори. _("nginx security headers пред zabobovdol/medqr"; verified; "https://nginx.org/en/docs/http/ngx_http_headers_module.html")_
- **2026-07-24:** pg_basebackup инкрементален бекъп (-i old_manifest_file, --incremental): пази само блокове, променени спрямо референтния; НЕ може да се възстанови директно — задължително pg_combinebackup за сглобяване с предходните бекъпи, от които зависи. -X none при нисък write трафик може дълго да чака WAL switch → pg_switch_wal на primary го форсира. _("Postgres инкрементални базови бекъпи (PG17+)"; verified; "https://www.postgresql.org/docs/current/app-pgbasebackup.html")_
- **2026-07-24:** pg_basebackup --manifest-checksums по подразбиране = CRC32C (бърз, лови случайни грешки, но НЕ е устойчив на злонамерена промяна); за tamper-evidence задай SHA256+. Манифестът винаги носи SHA256 на собственото си съдържание. Верификация: pg_verifybackup срещу манифеста; манифестът трябва да се пази отделно/сигурно, иначе атакуващ го преправя. _("Postgres физически бекъп интегритет"; verified; "https://www.postgresql.org/docs/current/app-pgbasebackup.html")_
- **2026-07-24:** pg_dump дъмпва САМО една база и НЕ включва роли/пароли и tablespaces (те са cluster-wide). За пълен бекъп на клъстера трябва pg_dumpall (или pg_dumpall --globals-only за само роли+tablespaces покрай per-DB pg_dump). Бекъп само с pg_dump губи GRANT-ите/потребителите при restore на чист клъстер. _("Postgres бекъп/restore пълнота"; verified; "https://www.postgresql.org/docs/current/backup-dump.html")_
- **2026-07-24:** pg_dump: 'directory' формат (-Fd) е ЕДИНСТВЕНИЯТ, който поддържа паралелни дъмпове (-j njobs); custom (-Fc) и directory са компресирани по подразбиране (gzip), tar (-Ft) НЕ поддържа компресия и не позволява пренареждане на реда при restore. Паралелният -j иска synchronized snapshots (multiple connections, консистентност). _("Postgres логически бекъп на zabobovdol/supreme"; verified; "https://www.postgresql.org/docs/current/app-pgdump.html")_
- **2026-07-24:** Ubuntu Security Guide (USG) прилага CIS и DISA-STIG втвърдяване през 'usg audit'/'usg fix' (canonical профили за 24.04/22.04). Инструмент за базово CIS L1 втвърдяване на хоста. _("hardening"; verified; "https://ubuntu.com/security/certifications/docs/usg")_
- **2026-07-24:** CrowdSec архитектура: детекцията (Security Engine/Log Processor + AppSec/WAF scenarios) е РАЗДЕЛЕНА от enforcement-а (Remediation Components/bouncers); участието дърпа curated community blocklist. Web слой — CrowdSec; бързи локални SSH бани — fail2ban. _("ips/hardening"; verified; "https://docs.crowdsec.net/docs/intro")_
- **2026-07-24:** sops криптира само стойностите (запазва структурата/ключовете четими за diff/merge) + MAC за интегритет; backend-и: age, AWS/GCP/Azure/HuaweiCloud KMS, PGP. За малък VPS: sops+age, decrypt при деплой в mode 600. _("secrets"; verified; "https://github.com/getsops/sops/blob/main/README.rst")_
- **2026-07-24:** cosign по подразбиране е keyless (Fulcio CA + Rekor transparency log, OIDC identity — без дълготрайни ключове). verify ИЗИСКВА --certificate-identity + --certificate-oidc-issuer, иначе гейтът е фиктивен. _("supply-chain"; verified; "https://github.com/sigstore/cosign/blob/main/README.md")_
- **2026-07-24:** Trivy текущ = 0.72.0 (2026-06-30) за supply-chain скан на образи/IaC/секрети в CI преди деплой. _("supply-chain"; verified; "https://github.com/aquasecurity/trivy/blob/main/CHANGELOG.md")_
- **2026-07-24:** Caddy master е на Go 1.25.1; automatic HTTPS = без downtime подновяване + авто HTTP→HTTPS редирект, серт. от LE или ZeroSSL, локална CA през Smallstep libs. По-малко движещи се части от certbot+nginx. _("tls/proxy"; verified; "https://caddyserver.com/docs/automatic-https")_
- **2026-07-24:** restic текущ = 0.19.1 (2026-07-05); 0.19.0 беше 2026-06-09. За бекъп верификация ползвай restic check + реален test-restore (бекъп без тестван restore не е бекъп). _("backup"; verified; "https://github.com/restic/restic/blob/master/CHANGELOG.md")_
- **2026-07-24:** OpenSSH 10.3 (2026-04-02) добави 'invaliduser' наказание към PerSourcePenalties и премина към floating-point time за наказанията — по-агресивна авто-защита срещу SSH сканиране на sshd ниво. _("ssh/hardening"; verified; "https://www.openssh.com/releasenotes.html")_
- **2026-07-24:** OpenSSH текущ = 10.4 (2026-07-06); въвежда post-quantum composite подпис mldsa44-ed25519 (ssh-keygen -t mldsa44-ed25519, combines ML-DSA-44 + Ed25519). _("ssh"; verified; "https://www.openssh.com/releasenotes.html")_
- **2026-07-24:** Docker 29.6.1 (2026-06-26) фиксира: злонамерен image с крафтнат /etc/passwd|/etc/group причинява OOM; и build request който изключва Seccomp. Ъпдейтвай, не отслабвай seccomp. _("docker/security"; verified; "https://docs.docker.com/engine/release-notes/29/")_
- **2026-07-24:** Docker Engine текущ стабилен = 29.6.2 (2026-07-16); фиксира CVE-2026-15788..15793 (BuildKit: command injection от git bundle, изтриване на /tmp, bypass на destination dir, NTFS junction escape). Носи containerd v2.2.6 + Go 1.26.5. Дръж Engine ≥29.6.2. _("docker/security"; verified; "https://docs.docker.com/engine/release-notes/29/")_
- **2026-07-24:** ARI: сървърът контролира честотата на проверка чрез Retry-After; при временна грешка клиентът SHOULD прави exponential backoff (backoff при грешка има приоритет над Retry-After). _("tls/acme"; verified; "https://www.rfc-editor.org/rfc/rfc9773.txt")_
- **2026-07-24:** ARI е стандартизиран в RFC 9773 (директория поле 'renewalInfo'). Клиентът SHOULD дърпа RenewalInfo веднага след издаване и после периодично; изборът кога да подновиш = избери равномерно РАНДОМ момент в suggestedWindow (не фиксирано). _("tls/acme"; verified; "https://www.rfc-editor.org/rfc/rfc9773.txt")_
- **2026-07-24:** LE профил tlsclient е премахнат от 2026-07-08 (беше идентичен на classic + TLS Client Auth EKU). Не го конфигурирай. _("tls/acme"; verified; "https://letsencrypt.org/docs/profiles/")_
- **2026-07-24:** Let's Encrypt НЕ поддържа OCSP; отзоваването е само през CRL или достатъчно кратка валидност. Не разчитай на OCSP stapling за LE серт. _("tls"; verified; "https://letsencrypt.org/docs/profiles/")_
- **2026-07-24:** Auth reuse таван по BR: max 200 дни, пада на 100 дни от 2027-03-15 и 10 дни от 2029-03-15. tlsserver реже pending authorization до 1 час и reuse до 7 часа (защото CAA recheck е на 8ч). _("tls/acme"; verified; "https://letsencrypt.org/docs/profiles/")_
- **2026-07-24:** КОРЕКЦИЯ на памет: реалните BR тавани за валидност падат на 100 дни от 2027-03-15 и на 47 дни от 2029-03-15 (не '45 дни от 13.05.2026'). tlsserver вече дава 45 дни в подготовка за 47-дневния таван. _("tls/acme"; verified; "https://letsencrypt.org/docs/profiles/")_
- **2026-07-24:** LE профил shortlived = ~6-дневни серт.; квалифицират се като Short-Lived Subscriber Certificates по BR → не носят revocation инфо (още имат CRL URL днес, може да отпадне). Само за напълно доверена автоматизация. _("tls/acme"; verified; "https://letsencrypt.org/docs/profiles/")_
- **2026-07-24:** LE профил tlsserver дава 45-дневни серт.; маха Common Name, Key Encipherment KU и Subject Key ID (по-малки серт., пълна автоматизация). Профил classic (default) = 90 дни. _("tls/acme"; verified; "https://letsencrypt.org/docs/profiles/")_
- **2026-07-22:** tools/vps/deploy-check.mjs е орфан — grep 'deploy-check' в .github/, tools/agents/, deploy/, .claude/ дава 0 съвпадения; референция има само в собствения tools/vps/README.md, който при това фалшиво твърди '(в агентския гейт)' и 'в CI sweep-а'. Инструментът е тестван (deploy-check.test.mjs) но никъде незакачен. _("статус на deploy-check.mjs в потока / drift код↔док"; verified; "grep -rl deploy-check /home/user/Few-few/.github /home/user/Few-few/tools/agents → празно (2026-07-22); Grep в deploy/ и .claude/ → 0; tools/vps/README.md:6,16")_
- **2026-07-17:** docker-entrypoint.sh P3005 gate е коректен: migrate deploy → при провал grep P3005 → само тогава resolve --applied 0_init + повторен deploy; всяка друга грешка exit 1 без resolve (не вписва baseline фалшиво). _(zabobovdol-deploy; verified; "zabobovdol/docker-entrypoint.sh:31-42 + sh -n OK, 2026-07-17")_
- **2026-07-17:** Изходящият agent proxy прави TLS interception (issuer=Anthropic Egress Gateway SDS), затова s_client от sandbox-а НЕ показва реалния Let's Encrypt сертификат на домейна — TLS валидността се проверява само от самия VPS или външен монитор. _(general; verified; "openssl s_client zabobovdol.carbonstealth.eu:443 → issuer O=Anthropic, 2026-07-17")_
- **2026-07-17:** Live /api/health на zabobovdol връща 404 (text/html) → health route от PR #52 още не е в продукция; сайтът е на пред-PR#52 билд (nginx/1.28.3, 200 на /). _(zabobovdol-deploy; verified; "curl -w '%{http_code}' https://zabobovdol.carbonstealth.eu/api/health → 404, 2026-07-17")_
- **2026-07-17:** zabobovdol autodeploy пуска scripts/deploy.sh с cwd=/opt/few-few/releases/<TS>/zabobovdol, а docker-compose.yml монтира ./backups релативно → дневните+pre-deploy дъмпове падат в release-локална backups/ и се трият с release пруна (KEEP_RELEASES=5, autodeploy.sh:709). db-data оцелява (named volume), бекъпите не. _(zabobovdol-deploy; verified; "deploy/autodeploy.sh:159-167,709 + zabobovdol/docker-compose.yml:103-106 (прочетени)")_
- **2026-07-16:** Deploy пренасяне на SMTP/секрети е коректно: Nexus държи .env в /opt/nexus/state/.env (mode 600), rsync го изключва (--exclude server/.env), symlink към source/.env + source/server/.env, съществуващ .env никога не се презаписва. eternaltouch пренася .env от текущия release. Известен пропуск: авто-генерираният eternaltouch .env блок (autodeploy.sh:511-517) НЕ включва SMTP_TLS_SERVERNAME — при първи деплой операторът трябва да го добави ръчно заедно със SMTP_PASS, иначе първото пращане гърми с cert-mismatch. _("deploy pipeline — пренасяне на SMTP секрети"; verified; "deploy/autodeploy.sh:326-356 (nexus), :487-523 (eternaltouch), :511-517 (пропуснат SMTP_TLS_SERVERNAME)")_
- **2026-07-16:** Register.it SMTP (authsmtp.register.it) канонична конфигурация за всеки нов продукт: (1) порт 587 STARTTLS submission по подразбиране, secure:false — 465 implicit TLS е блокиран изходящо на Hetzner и много хостове, 587 практически винаги отворен; override към 465+SMTP_SECURE=true само ако 587 не минава. (2) authsmtp.register.it представя TLS сертификат за *.securemail.pro / smtp.webnode.com, НЕ за connect хоста → задай SMTP_TLS_SERVERNAME=smtp.securemail.pro, за да остане rejectUnauthorized=true (НИКОГА не изключвай валидацията). (3) requireTLS:true на 587, иначе активен MITM сваля STARTTLS от EHLO и AUTH LOGIN креденшълите изтичат plaintext. (4) SMTP_USER/SMTP_PASS само в .env на сървъра (mode 600), никога в репо/архив. _("SMTP/имейл за всеки продукт с Register.it (Nexus, eternaltouch и бъдещи)"; verified; "Nexus/server/src/lib/email.ts:22-53, eternaltouch/src/lib/email.js:9-52, Nexus/.env.example:33-49")_
- **2026-07-14:** mastilko билдът е тежък — layout.tsx импортва ~60 next/font/google фамилии наведнъж; на 4GB VPS next build пада 'Killed' (OOM). 2G swap (fallocate/mkswap/swapon) го оправя. (Точните числа 61 шрифта/25 стр. са приблизителни: реално ~60 фамилии, 15 page route-а.) _("билд ресурси на малък VPS"; verified; "mastilko/src/app/layout.tsx:2-13 (~60 фамилии); glob 15 page.tsx; OOM/swap = серверно наблюдение")_
- **2026-07-14:** mastilko next.config.mjs праща HSTS (max-age=63072000; includeSubDomains). При симптом 'не сигурно' в браузъра след валиден LE cert + curl -sSI https без -k дава ssl_verify_result: 0 → проблемът е запомнен per-site cert override в Chrome (DevTools Security: 'you have recently allowed...'), не сървърен. Лек: инкогнито/рестарт на браузъра, не сървърна промяна. _("TLS диагностика — клиентски кеш vs сървър"; verified; "mastilko/next.config.mjs:25-26 (HSTS хедър); браузър override = операционно наблюдение")_
- **2026-07-14:** autodeploy find_archive взима най-новия по mtime архив (ls -t *.zip *.tar.gz | head -1); стар архив в /root → грешен деплой. За детерминиран деплой: ARCHIVE=/root/<точно>.zip PROJECTS=mastilko bash autodeploy.sh. _("autodeploy избор на архив/проект"; verified; "deploy/autodeploy.sh:95,98,24")_
- **2026-07-14:** autodeploy.sh презаписва само главния unit /etc/systemd/system/mastilko.service, не пипа .d/ — grep за override/.d в скрипта = 0 съвпадения. Затова drop-in /etc/systemd/system/<svc>.service.d/override.conf оцелява деплой. За спешна прод поправка на unit → drop-in, не редакция на файла (иначе autodeploy я връща). _("systemd unit hotfix при autodeploy"; verified; "deploy/autodeploy.sh (grep override|.d/ → 0); mastilko.service:1-3")_
- **2026-07-14:** mastilko.service ползва САМО seccomp денилист (SystemCallFilter=~@mount @swap @reboot @obsolete @module @raw-io @debug), защото allowlist-ът @system-service убиваше Node с SIGSYS на прод ядрото (V8/libuv ползват syscalls извън списъка, напр. clone3). Задължителен и AF_NETLINK в RestrictAddressFamilies — libuv чете мрежовите интерфейси през netlink. За Next/Node systemd unit: денилист, не allowlist. _("systemd hardening на Node/Next сервиси"; verified; "mastilko/deploy/mastilko.service:48-52 (коментари ред 49-51 обясняват причината)")_
- **2026-07-11:** Merge на origin/main в mastilko feature клона докосна deploy/autodeploy.sh (+210 реда), но само за чужди продукти — deploy_mastilko() тялото е непроменено; единствената mastilko-промяна е че PROJECTS default вече включва 'eternaltouch adblock' СЛЕД mastilko. При пълен autodeploy без PROJECTS= сега се опитват и тези два проекта; за само-mastilko деплой ползвай `PROJECTS=mastilko bash autodeploy.sh`. mastilko е пръв в списъка, тъй че провал на новите не го застрашава. _("autodeploy.sh PROJECTS список / mastilko изолация при merge"; verified; "git diff ae4b591d^1 ae4b591d -- deploy/autodeploy.sh (grep -i mastilko → само PROJECTS ред + hunk headers); git diff --stat на mastilko/deploy/ = празно")_
- **2026-07-11:** mastilko/ Vizitka-импорт е чисто клиентски: VizitkaImport.tsx е 'use client' и fetch(vizitkaApiUrl) тече в браузъра, не на сървъра. Следствие за деплоя — няма сървърен egress към vizitka-bg.com, systemd sandbox-ът е незасегнат; единственото изискване е CSP да разреши произхода (img-src+connect-src), което next.config.mjs прави с fallback https://vizitka-bg.com = прод стойността. Никаква нова задължителна env/тайна. _("mastilko деплой / Vizitka-импорт функция"; verified; "жив grep: mastilko/src/components/VizitkaImport.tsx:1,28 (use client + fetch); mastilko/src/lib/vizitka-import.ts (само URL util); mastilko/next.config.mjs:6-9,40-42 (VIZITKA_ORIGIN fallback в CSP); mastilko/.env.example (NEXT_PUBLIC_VIZITKA_URL незадължителна)")_
- **2026-07-11:** mastilko на ПЪРВО пускане (без .env) билдва с коректен CSP: NEXT_PUBLIC_VIZITKA_URL пада на fallback https://vizitka-bg.com, който съвпада с продукционната стойност. Затова липсата на .env при първия билд не чупи CSP, а health на / връща 200 без никакви тайни (SESSION_SECRET, GEMINI_API_KEY не са нужни за буут; /admin → 307, не гърми). _("mastilko първо пускане / build-time NEXT_PUBLIC_* fallback"; verified; "жив CSP хедър от next start (img-src/connect-src = https://vizitka-bg.com); /home/user/Few-few/mastilko/next.config.mjs:7-9; /home/user/Few-few/mastilko/src/middleware.ts")_
- **2026-07-11:** Когато Next.js сервира статиката от public/ и nginx само проксира към порта (proxy_pass), nginx НЯМА нужда от .webp MIME конфиг — Next връща коректно Content-Type: image/webp. MIME конфиг в nginx трябва само ако nginx сервира файловете директно (root/alias). _("Next.js/Node app зад nginx reverse proxy, статика от public/"; verified; "curl на http://127.0.0.1:3200/icons/etiketi.webp и /mascot.webp → 200 type=image/webp; /home/user/Few-few/mastilko/deploy/nginx-mastilko.conf (само proxy_pass)")_
- **2026-07-11:** mastilko: всеки <Image> носи изрично `unoptimized` prop (Logo.tsx, Header.tsx, page.tsx, 8-те tool страници) и QrImage е обикновен <img> — затова sharp НЕ е нужен нито при `next build`, нито при `next start`, въпреки че next.config.mjs няма глобален images.unoptimized. `npm prune --omit=dev` е безопасен (next е prod-dep, config е .mjs → без typescript при старт). _("Next.js 15 продукт с unoptimized изображения зад systemd (mastilko)"; verified; "жив тест на машината: `next build` (25 стр., без sharp warning) + `next start` на 3200 → HTTP 200; grep на всички next/image употреби в /home/user/Few-few/mastilko/src")_
- **2026-07-09:** Под set -euo pipefail редът VAR="$(grep '^KEY=' file | cut -d= -f2)" убива скрипта ТИХО, когато grep няма съвпадение: pipefail прави pipeline-а 1, статусът на чистото присвояване е статусът на command substitution-а → set -e изход. Дефолт "${VAR:-x}" на СЪЩИЯ ред не спасява (не се стига). Фикс: '... || true' вътре в substitution-а. _("bash деплой скриптове, четящи опционални ключове от .env"; verified; "възпроизведено: scratchpad t1.sh (bash, exit=1, редът след него не се изпълнява) срещу infra/autodeploy.sh:111")_
- **2026-07-08:** При SQLite приложение, което държи качените файлове като отделни файлове в data/uploads (не в базата), бекъпът трябва да включва И tar на uploads/, не само .backup на базата — за разлика от medqr, който пази снимките в базата. _("SQLite systemd продукти с файлови качвания (vizitka)"; verified; "/home/user/Few-few/vizitka/src/db.js:6-9 (UPLOADS_DIR = data/uploads)")_
- **2026-07-08:** nginx client_max_body_size трябва да надвишава най-големия multer fileSize лимит + multipart overhead, иначе nginx връща 413 ПРЕДИ приложението да види заявката. Vizitka: банер 3MB (src/routes/admin.js:23) → client_max_body_size 4m, не 3m. _("nginx reverse proxy пред Express/multer app с качване на файлове"; verified; "/home/user/Few-few/vizitka/src/routes/admin.js:23 ; /home/user/Few-few/vizitka/deploy/nginx/vizitka.conf")_
- **2026-07-08:** В mastilko деплоя CSP зависи от NEXT_PUBLIC_VIZITKA_URL при БИЛД (autodeploy билдва на сървъра, Next авточете /opt/mastilko/.env) — env-ът трябва да е на място преди npm run build, иначе CSP пада на fallback. _("build-time NEXT_PUBLIC_* и CSP синхрон"; verified; "/home/user/Few-few/deploy/autodeploy.sh:286-289 ; /home/user/Few-few/mastilko/next.config.mjs")_
- **2026-07-08:** Мастилко /import (client-side fetch към vizitka-bg.com) и рендирането на отдалечен photo_url като <img> изискват vizitka origin в CSP connect-src И img-src (next.config.mjs). Deploy health на / връща 200 дори когато функцията е счупена от CSP — health check не хваща blocked cross-origin fetch. _("mastilko CSP / cross-origin функции зад health-check деплой"; verified; "/home/user/Few-few/mastilko/src/components/VizitkaImport.tsx:28 ; /home/user/Few-few/mastilko/src/lib/vizitka-import.ts:11 ; /home/user/Few-few/mastilko/next.config.mjs:22-27")_
- **2026-07-06:** pgBackRest дава PITR (target time/xid), WAL archiving, repo encryption, retention (full/diff/archive), block incremental + file bundling (по-малко I/O при много малки файлове) и S3/Azure/GCS/SFTP таргети. За прод RPO превъзхожда pg_dump; restic остава за offsite на файловете. _("Postgres (zabobovdol db) бекъп с PITR вместо само pg_dump"; verified; "https://pgbackrest.org/user-guide.html")_
- **2026-07-06:** SOPS (CNCF sandbox) криптира YAML/JSON/ENV/INI/BINARY с backend-и age/PGP/AWS-GCP-Azure KMS. age има малки explicit ключове и по README post-quantum support; age-keygen дава публичен recipient. Модел за нас: sops+age криптирано в репо → decrypt при деплой в mode 600. _("secret management извън plain .env, тайни в репото криптирани"; verified; "https://github.com/getsops/sops/blob/main/README.rst ; https://github.com/FiloSottile/age/blob/main/README.md")_
- **2026-07-06:** SBOM+scan пайплайн: syft генерира SBOM (CycloneDX/SPDX/Syft-JSON, in-toto signed attestations); grype чете SBOM директно за по-бърз vuln scan и дава EPSS/KEV/risk scoring + OpenVEX filtering; trivy е по-широк (image/fs/git/VM/k8s + IaC misconfig + secrets + licenses). Комбинирай: syft -o cyclonedx-json | grype sbom:-. _("supply-chain гейт в autodeploy преди up на образ"; verified; "https://github.com/anchore/syft/blob/main/README.md ; https://github.com/anchore/grype/blob/main/README.md ; https://github.com/aquasecurity/trivy/blob/main/README.md")_
- **2026-07-06:** Distroless образите нямат shell/package manager, затова Dockerfile ENTRYPOINT ЗАДЪЛЖИТЕЛНО във vector форма [\"app\"] (иначе runtime търси shell). static-debian13 ~2 MiB. Всички distroless са cosign keyless подписани (verify: issuer https://accounts.google.com, identity keyless@distroless.iam.gserviceaccount.com). _("multi-stage hardened образи за app контейнерите"; verified; "https://github.com/GoogleContainerTools/distroless/blob/main/README.md")_
- **2026-07-06:** OWASP CRS paranoia levels: PL1 почти никога не дава false positives (прод default), PL2+ хваща повече атаки, но и повече фалшиви на легитимен трафик. Тунинг: старт DetectionOnly + exclusions, после blocking, вдигай PL постепенно. _("конфигуриране на CRS без да чупиш легитимен трафик"; verified; "https://coreruleset.org/docs/2-how-crs-works/2-2-paranoia_levels/")_
- **2026-07-06:** OWASP Coraza е Go WAF, чете ModSecurity SecLang и е 100% съвместим с OWASP CRS. Зрялост на интеграциите: Caddy плъгин stable, proxy-wasm/Envoy stable, Traefik preview, Nginx/Apache experimental. За нов деплой предпочитай Caddy+Coraza пред libmodsecurity3. _("WAF на edge за zabobovdol/medqr зад прокси"; verified; "https://coraza.io/docs/tutorials/introduction/")_
- **2026-07-06:** systemd sandboxing директиви за услуги (потвърдено съществуване в systemd.exec): NoNewPrivileges, ProtectSystem, ProtectHome, PrivateTmp, PrivateDevices, ProtectKernelTunables/Modules/Logs, ProtectControlGroups, RestrictAddressFamilies, RestrictNamespaces, RestrictRealtime, RestrictSUIDSGID, MemoryDenyWriteExecute, LockPersonality, SystemCallFilter, CapabilityBoundingSet, ProtectClock, ProtectHostname, ProtectProc. Комбинирай ги за medqr unit-а (цел systemd-analyze security < 5). _("systemd hardening на medqr"; verified; "https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html")_
- **2026-07-06:** restic: репо е криптирано и дедуплицирано по подразбиране. Периодична проверка на целостта: 'restic check' (метаданни), 'restic check --read-data' (пълна верификация на данните). Бекъп без тестван check/restore не е бекъп. _("бекъп стратегия 3-2-1 с restic"; verified; "https://restic.readthedocs.io/en/stable/010_introduction.html")_
- **2026-07-06:** CrowdSec: behavior-based scenarios анализират логове/HTTP заявки; при участие получаваш curated community blocklist от валидирани атакуващи. Enforcement е през отделни Remediation Components (bouncers) на ниво iptables/nftables/firewall/reverse-proxy/webserver. Модел: детекция отделена от блокиране — по-подходящ за уеб слоя от fail2ban. _("IPS/WAF слой срещу автоматизиран recon"; verified; "https://docs.crowdsec.net/docs/intro")_
- **2026-07-06:** unattended-upgrades капан: конфигът е в /etc/apt/apt.conf.d/20auto-upgrades (периодичност) + 50unattended-upgrades (Allowed-Origins, Automatic-Reboot). На minimal Ubuntu Automatic-Reboot \"true\" НЕ работи без инсталиран пакет update-notifier-common — тогава нито рестартира, нито дори уведомява. Проверявай с 'apt-config dump' (файловете се четат лексикографски). _("автоматични security ъпдейти Ubuntu 24.04"; verified; "https://help.ubuntu.com/community/AutomaticSecurityUpdates")_
- **2026-07-06:** Caddy automatic HTTPS е включен по подразбиране: сам взима и подновява сертификати (Let's Encrypt/ZeroSSL) и прави HTTP(80)→HTTPS(443) redirect без downtime; никога не презаписва явна конфигурация, само я допълва. По-малко движещи се части от certbot+nginx. _("избор на reverse proxy / TLS автоматика"; verified; "https://caddyserver.com/docs/automatic-https")_
- **2026-07-06:** nginx HTTP/3: модул ngx_http_v3_module наличен от версия 1.25.0. Конфиг: 'listen 443 quic reuseport;' + 'listen 443 ssl;' на същия порт, 'http3 on;' и 'add_header Alt-Svc \"h3=\\\":443\\\"; ma=86400\";' за да се рекламира h3. Изисква QUIC-съвместим OpenSSL/BoringSSL build. _("nginx edge с HTTP/3"; verified; "https://nginx.org/en/docs/http/ngx_http_v3_module.html")_
- **2026-07-06:** OpenSSH 10.3 (2026-04-02) добавя 'invaliduser' penalty към PerSourcePenalties (бан на IP при опити с несъществуващ юзър). OpenSSH 10.0 прави mlkem768x25519-sha256 (post-quantum hybrid) стандартен KEX. Втвърди: PerSourcePenalties + invaliduser вместо/заедно с fail2ban за SSH. _("SSH втвърдяване"; verified; "https://www.openssh.com/releasenotes.html")_
- **2026-07-06:** Docker Engine 29.x (най-нов 29.6.1, 2026): containerd image store е СТАНДАРТНИЯТ storage backend (не overlay2 graphdriver). Поддържа multi-platform images и има автоматична миграция. _("версия/поведение на Docker Engine"; verified; "https://docs.docker.com/engine/storage/containerd/")_
- **2026-07-06:** Docker Compose secrets се монтират като файл в /run/secrets/<name> per-service (granular достъп през filesystem права), за разлика от env vars които текат в логове и са видими за всички процеси. Дефинирай top-level secrets: {file:} или {environment:}; images като postgres/mysql ползват конвенцията *_FILE. _("тайни в zabobovdol Compose стек"; verified; "https://docs.docker.com/compose/how-tos/use-secrets/")_
- **2026-07-06:** Docker rootless mode: daemon + контейнери работят в user namespace като non-root, без SETUID binaries/file capabilities — смекчава уязвимости в daemon-а и runtime-а. Инсталира се с dockerd-rootless-setuptool.sh (изисква newuidmap/newgidmap, пакет docker-ce-rootless-extras); DOCKER_HOST=unix:///run/user/UID/docker.sock. _("втвърдяване на Docker хост"; verified; "https://docs.docker.com/engine/security/rootless/")_
- **2026-07-06:** ARI (ACME Renewal Information): ACME клиентът трябва да проверява ARI поне веднъж дневно; shortlived (6-дневни) сертификати се подновяват на всеки 2–3 дни; ARI позволява на CA да нареди ранно подновяване. Затова renew на ~⅓ остатъчен живот + чест cron, не фиксирани 60 дни. _("автоматизирано подновяване на TLS"; verified; "https://letsencrypt.org/2025/02/20/first-short-lived-cert-issued")_
- **2026-07-06:** Let's Encrypt приключи OCSP: OCSP URL-и махнати от сертификатите на 07.05.2025, OCSP responders изключени на 06.08.2025 (revocation само през CRL). Следствие: ssl_stapling / OCSP stapling за LE сертификати е безсмислен — премахни го от nginx/Caddy конфига. _("nginx/Caddy TLS конфиг за Let's Encrypt"; verified; "https://letsencrypt.org/2024/12/05/ending-ocsp/")_
- **2026-07-06:** Let's Encrypt ACME профили: classic=90 дни (default, избира се автоматично), tlsserver=45 дни, shortlived=~6 дни. tlsserver/shortlived изискват ACME клиент с поддръжка на certificate profiles (напр. lego); shortlived сертификатите НЯМАТ OCSP/CRL URL (не се нуждаят от revocation). _("TLS/ACME планиране на профил и живот на сертификата"; verified; "https://letsencrypt.org/docs/profiles/")_
- **2026-07-03:** Build-from-source Dockerfile на pnpm workspace: COPY-вай package.json на ВСЯКА workspace зависимост, която tsc на таргета достига (web/src/dev импортира game-core → TS2307 само в Docker, локално минава) + билдвай я преди таргета; dev-only импорти също се typecheck-ват. _("pnpm monorepo Docker билдове"; verified; "infra/Dockerfile.web (game-core COPY + build)")_
- **2026-07-03:** docker compose -f infra/docker-compose.yml взима project name от ДИРЕКТОРИЯТА на compose файла ("infra"), не от cwd — стар стек от предишен опит държи портовете, а pg volume-ът пази СТАРАТА парола: при нови тайни задължително down -v преди up, иначе api-то не може да се върже. _("compose деплой на споделен VPS"; verified; "infra/docker-compose.yml (deploy сесия 2026-07-02)")_
- **2026-07-03:** nginx: два site файла, дефиниращи един и същ upstream (aso_api), дават emerg "duplicate upstream" и събарят и certbot --nginx — преди добавяне на нов конфиг провери sites-enabled за останки от предишни опити; предпочети презапис на съществуващия файл (пази 443 блока на certbot). _("nginx edge на споделен хост"; verified; "/etc/nginx/sites-enabled/gaming.carbonstealth.eu (deploy сесия)")_
- **2026-07-03:** Първи OWNER на чиста прод база: env BOOTSTRAP_OWNER_EMAIL — api-то при boot идемпотентно промотира този имейл (updateMany + audit); без него всяка свежа база иска ръчен SQL. _("bootstrap на прод среда"; verified; "apps/api/src/env.ts + apps/api/src/index.ts (bootstrapOwner)")_
- **2026-07-02:** Приеми автоматизиран AI-driven recon срещу инфраструктурата ни (фреймуърци като HexStrike AI автоматизират 150+ Kali инструмента). Отбрана: авторизирано self-scanning на СОБСТВЕНАТА инфра (nmap/nikto срещу нашия VPS) като част от втвърдяването; дръж fail2ban, rate-limit, TLS и пачовете на зависимостите стегнати; минимизирай exposed портове/банери; никакви default creds. _("hardening срещу автоматизиран AI recon"; verified; "https://github.com/0x4m4/hexstrike-ai (автономен пентест MCP, 150+ tools) — defensive извод")_
- **2026-07-02:** Несъответствие state-модел: autodeploy bind-symlink-ва data към release tree, но compose ползва named volume → реалната SQLite е в /var/lib/docker/volumes, не в state dir; бекъпите таргетват грешно място. Уеднаквявай (bind mount) или бекъпвай през docker compose exec. _("SQLite бекъп в Docker Compose деплой"; verified; "/home/user/Few-few/Nexus/docker-compose.yml (nexus-dominion_data volume) vs /home/user/Few-few/deploy/autodeploy.sh:193-212")_
- **2026-07-02:** Docker Compose интерполира ${VAR:?} от .env в директорията на compose файла (project dir) или от shell — НЕ от env_file и НЕ от произволен път. Symlink на тайните към server/.env не помага на interpolation в docker-compose.yml, който е в корена на проекта; трябва project-root .env. _("docker compose deploy; wiring secrets via autodeploy state dir"; verified; "https://docs.docker.com/compose/how-tos/environment-variables/variable-interpolation/")_
- **2026-06-26:** Compose network с internal: true създава външно изолирана мрежа (без външна свързаност) — слагай db в internal, а само reverse-proxy в споделена с външния свят, за да няма пряк път db към интернет _(docker internal network; verified; https://docs.docker.com/reference/compose-file/networks/)_
- **2026-06-26:** Docker named volume се управлява от Docker (под /var/lib/docker/volumes), изолиран от хоста, по-лесен за бекъп/миграция и по-безопасен от bind mount, който зависи от пътя и структурата на хост FS _(docker volume vs bind mount; verified; https://docs.docker.com/engine/storage/volumes/)_
- **2026-06-26:** logrotate copytruncate копира лога и после truncate-ва оригинала на място (за процеси, които не затварят файла) — има прозорец между копиране и truncate, в който може да се загубят редове; при copytruncate опцията create няма ефект _(logrotate copytruncate; verified; https://linux.die.net/man/8/logrotate)_
- **2026-06-26:** systemd timer Persistent=true (OnCalendar) изпълнява пропуснат таймер веднага при включване ако моментът е минал докато машината е изключена — за разлика от cron; RandomizedDelaySec разпръсва едновременно стартиращи таймери _(systemd timer vs cron; verified; https://www.freedesktop.org/software/systemd/man/latest/systemd.timer.html)_
- **2026-06-26:** journald Storage=volatile държи лога само в RAM; persistent (под /var/log/journal) се изисква за per-user журнали и FSS; journald стартира volatile до systemd-journal-flush.service; RateLimitIntervalSec/Burst глушат сервиз над прага (override per-unit LogRateLimit*) _(journald storage/ratelimit; verified; https://www.freedesktop.org/software/systemd/man/latest/journald.conf.html)_
- **2026-06-26:** nginx proxy_cache_use_stale updating сервира остаряло кеширано съдържание докато един работник опреснява записа; с proxy_cache_background_update on опресняването е във фон, а клиентът получава stale моментално _(nginx proxy_cache stale; verified; https://nginx.org/en/docs/http/ngx_http_proxy_module.html)_
- **2026-06-26:** Caddy по подразбиране ползва два ACME CA (Let's Encrypt и ZeroSSL) с автоматичен fallback; при повторни грешки минава към staging на Let's Encrypt за да пести rate limit; wildcard изисква DNS challenge _(caddy ACME issuers; verified; https://caddyserver.com/docs/automatic-https)_
- **2026-06-26:** Caddy on-demand TLS взима сертификат при първия TLS handshake за непознато SNI; ЗАДЪЛЖИТЕЛНО се ограничава с глобален ask HTTP endpoint, който Caddy пита дали има право за домейна — иначе е отворен за злоупотреба _(caddy on-demand TLS; verified; https://caddyserver.com/docs/automatic-https)_
- **2026-06-26:** Postgres streaming репликация е асинхронна по подразбиране (прозорец за загуба при срив); синхронна се включва с synchronous_standby_names + synchronous_commit on; remote_write чака ОС-а на standby, remote_apply чака replay (видимост); ANY N = кворум, FIRST N = приоритет _(postgres sync replication; verified; https://www.postgresql.org/docs/current/warm-standby.html)_
- **2026-06-26:** Postgres логическа репликация НЕ репликира схема/DDL, sequence стойности и large objects; при failover към subscriber sequence-ите трябва ръчно да се вдигнат; адитивни схемни промени се прилагат първо на subscriber _(postgres logical replication; verified; https://www.postgresql.org/docs/current/logical-replication-restrictions.html)_
- **2026-06-26:** Postgres autovacuum НЕ издава ANALYZE за партиционирани и foreign таблици и не разпространява нагоре към родителя — за коректни планове трябва ръчно периодично ANALYZE; autovacuum никога не пуска VACUUM FULL _(postgres autovacuum analyze; verified; https://www.postgresql.org/docs/current/routine-vacuuming.html)_
- **2026-06-26:** Postgres transaction ID wraparound: всяка таблица трябва да се vacuum-ва поне веднъж на 2 млрд транзакции; на ~40 млн до wraparound идват WARNING, при под 3 млн системата отказва нови XID (read-only); решение е database-wide VACUUM (НЕ FULL, НЕ single-user) от superuser _(postgres XID wraparound; verified; https://www.postgresql.org/docs/current/routine-vacuuming.html)_
- **2026-06-26:** Postgres wal_level се сменя само при рестарт, но archive_command се презарежда с reload; за временно спиране на архивирането задай archive_command='' и WAL ще се трупа в pg_wal/ докато върнеш работещ archive_command _(postgres wal_level reload; verified; https://www.postgresql.org/docs/current/continuous-archiving.html)_
- **2026-06-26:** Postgres PITR: archive_command трябва да връща НЕНУЛЕВ изход при провал и да НЕ презаписва съществуващ файл (test ! -f dest && cp); ако archive_command се проваля или archive диск се напълни, pg_wal/ расте, а препълване води до PANIC спиране (без загуба на комитнати транзакции) _(postgres PITR archive_command; verified; https://www.postgresql.org/docs/current/continuous-archiving.html)_
- **2026-06-26:** Compose depends_on с condition: service_healthy стартира зависимия сервиз чак след като зависимостта мине healthcheck; service_started само чака контейнерът да тръгне; healthcheck полетата (interval, timeout, start_period, start_interval) са durations, start_interval иска Compose мин. 2.20.2 _(compose depends_on healthcheck; verified; https://docs.docker.com/reference/compose-file/services/)_
- **2026-06-26:** Kernel net.ipv4.conf.*.rp_filter използва МАКСИМУМА от conf/all и conf/iface — само all=1 може да се надделее; strict (1, RFC3704) при симетрично рутиране, loose (2) при asymmetric; tcp_syncookies е fallback срещу SYN flood, не средство легитимно претоварен сървър да удържа връзки _(kernel sysctl hardening; verified; https://docs.kernel.org/networking/ip-sysctl.html)_
- **2026-06-26:** Postgres: над 40% RAM в shared_buffers рядко е по-добро (разчита и на OS cache); по-голям shared_buffers иска по-голям max_wal_size; max_connections оразмерява вътрешни ресурси директно → за много клиенти ползвай pooler вместо да вдигаш max_connections _(postgres shared_buffers tuning; verified; https://www.postgresql.org/docs/current/runtime-config-resource.html)_
- **2026-06-26:** Postgres work_mem е лимит ПЪР ОПЕРАЦИЯ (sort/hash), не пър връзка: сложна заявка ползва няколко наведнъж по много сесии → реалната памет е кратна на work_mem; не го вдигай глобално високо при висок max_connections _(postgres work_mem; verified; https://www.postgresql.org/docs/current/runtime-config-resource.html)_
- **2026-06-26:** nginx limit_req_zone състояние заема 128 байта на 64-bit → 1MB зона пази около 8000 IP състояния; при изчерпване се изтласква LRU, после заявката пада с грешка — размери зоната спрямо очаквания брой уникални клиенти _(nginx limit_req sizing; verified; https://nginx.org/en/docs/http/ngx_http_limit_req_module.html)_
- **2026-06-26:** nginx ssl_early_data on (TLS 1.3 0-RTT) излага заявките на replay атаки; задължително подай proxy_set_header Early-Data $ssl_early_data към backend-а, за да отхвърли non-idempotent заявки в early data _(nginx TLS 1.3 0-RTT replay; verified; https://nginx.org/en/docs/http/ngx_http_ssl_module.html)_
- **2026-06-26:** nginx ssl_session_cache builtin (OpenSSL-вграден) се ползва само от 1 worker и причинява memory fragmentation — за многоядрен VPS винаги ползвай shared:NAME:SIZEm (1MB около 4000 сесии), споделен между всички workers _(nginx ssl_session_cache; verified; https://nginx.org/en/docs/http/ngx_http_ssl_module.html)_
- **2026-06-26:** systemd MemoryDenyWriteExecute=true предпочитано се прилага чрез prctl() kernel проверка (не само seccomp), блокирайки W^X: mmap PROT_EXEC|PROT_WRITE, mprotect към PROT_EXEC, shmat SHM_EXEC — спира JIT-style експлойти _(systemd MemoryDenyWriteExecute; verified; https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html)_
- **2026-06-26:** systemd RestrictAddressFamilies=none отказва всички address families; за HTTP сервиз заключи до точно AF_UNIX AF_INET AF_INET6 (allow-list) — затваря екзотични сокети (AF_PACKET, AF_NETLINK) като вектор _(systemd RestrictAddressFamilies; verified; https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html)_
- **2026-06-26:** systemd ProtectClock=true маха CAP_SYS_TIME И CAP_WAKE_ALARM, блокира clock-set syscalls изцяло и прави /dev/rtc read-only _(systemd ProtectClock; verified; https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html)_
- **2026-06-26:** systemd ProtectKernelModules=true маха CAP_SYS_MODULE от bounding set, инсталира syscall филтър срещу module load/unload И прави /usr/lib/modules недостъпна — три отделни механизма, не само capability _(systemd ProtectKernelModules; verified; https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html)_
- **2026-06-26:** systemd ProtectKernelTunables=true прави read-only /proc/sys, /sys, /proc/sysrq-trigger, /proc/irq, и прави /proc/kallsyms + /proc/kcore недостъпни — затваря изтичане на kernel symbol адреси _(systemd ProtectKernelTunables; verified; https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html)_
- **2026-06-26:** systemd ProtectControlGroups приема не само boolean, а и private (cgroup namespace с частен writable mount на /sys/fs/cgroup) и strict (частен read-only mount) — по-силна изолация от стария boolean; при включване MountAPIVFS се имплицира _(systemd unit hardening; verified; https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html)_
- **2026-06-26:** unattended-upgrades: dpkg-reconfigure --priority=low unattended-upgrades пише /etc/apt/apt.conf.d/20auto-upgrades; за авто-reboot трябва И Unattended-Upgrade::Automatic-Reboot "true" И инсталиран пакет update-notifier-common _(unattended-upgrades reboot; verified; https://help.ubuntu.com/community/AutomaticSecurityUpdates)_
- **2026-06-26:** Ubuntu SSH хардънинг: конфигурирай през snippet файлове в /etc/ssh/sshd_config.d/*.conf (override над главния заради Include), препоръчан ключ ed25519, рестарт systemctl restart ssh.service _(SSH хардънинг snippets; verified; https://documentation.ubuntu.com/server/how-to/security/openssh-server/)_
- **2026-06-26:** UFW по подразбиране е DISABLED; политиката по подразбиране е deny (incoming), allow (outgoing); статус с ufw status verbose _(UFW firewall default; verified; https://help.ubuntu.com/community/UFW)_
- **2026-06-26:** Certbot 4.0.0+: прагът за подновяване вече е ДИНАМИЧЕН (ARI; за серти с живот до 10 дни = половината от живота), не фиксираните 30 дни от по-старите версии _(Certbot ARI подновяване; verified; https://eff-certbot.readthedocs.io/en/stable/using.html)_
- **2026-06-26:** systemd: при ProtectSystem=strict + PrivateTmp= директориите /tmp и /var/tmp стават записваеми (частни), а DynamicUser= имплицитно включва NoNewPrivileges + RestrictSUIDSGID (не може да се изключат) _(systemd hardening unit; verified; https://www.freedesktop.org/software/systemd/man/latest/systemd.exec.html)_
- **2026-06-26:** Docker json-file (default logging driver) НЕ прави log rotation по подразбиране → диск може да се запълни; задай log-opts max-size/max-file в daemon.json или ползвай local драйвера, който ротира сам _(Docker log rotation disk; verified; https://docs.docker.com/engine/logging/configure/)_
- **2026-06-26:** Compose default файл е compose.yaml (предпочитан) или compose.yml; docker-compose.yaml/.yml се поддържат само за обратна съвместимост; при два файла печели compose.yaml _(Docker Compose файл именуване; verified; https://docs.docker.com/compose/intro/compose-application-model/)_
- **2026-06-26:** Docker Compose top-level version: е OBSOLETE — само информативен, Compose дава warning и валидира с най-новата схема; не го слагай в нови compose файлове _(Docker Compose схема; verified; https://docs.docker.com/reference/compose-file/version-and-name/)_
- **2026-06-26:** Let's Encrypt лимити (живо 2026): 50 серта/registered domain/7 дни, 5 дубликата/exact set от identifiers/7 дни, 300 нови orders/account на 3h; подновяванията са exempt _(ACME rate limits; verified; https://letsencrypt.org/docs/rate-limits/)_
- **2026-06-26:** Ubuntu LTS получава 5 г. standard security maintenance + разширено покритие (ESM/Ubuntu Pro) до общо 10 г. — Expanded Security Maintenance е отделна фаза след standard support _(Ubuntu LTS lifecycle; verified; https://ubuntu.com/about/release-cycle)_
- **2026-06-26:** nginx има ВТОРИ, по-ранен батч CVE отделен от 1.31.2/1.30.3: CVE-2026-42926 (buffer overflow в rewrite module), CVE-2026-42934 (HTTP/3 address spoofing), CVE-2026-42945 (buffer overread scgi/uwsgi), CVE-2026-40460 (resolver use-after-free в OCSP, уязвими 1.25.0-1.30.0), всички поправени в 1.31.0/1.30.1; плюс CVE-2026-9256 (HTTP/2 request injection) поправен в 1.31.1/1.30.2. Заключение: фронт-nginx ≥1.31.2 (или ≥1.30.3) покрива и двата батча 2026. Caddy не е засегнат. _("nginx reverse proxy пред zabobovdol/medqr — праг за ъпдейт"; verified; "https://nginx.org/en/security_advisories.html (живо 2026-06-26); cross-сверено с https://nginx.org/en/CHANGES (mainline 1.31.2, 17 Jun 2026, живо 2026-06-26)")_
- **2026-06-26:** trivy-action latest е v0.36.0 (2026-04-22). Action версията изостава далеч зад trivy CLI (v0.71.2) — не бъркай action tag с trivy binary версия при пиниране. _("пиниране на trivy версии в CI/деплой"; verified; "https://api.github.com/repos/aquasecurity/trivy-action/releases/latest (живо 2026-06-26)")_
- **2026-06-26:** dev-sec/ansible-collection-hardening (*5.4k) latest е 10.6.0 (2026-05-26) — battle-tested хардънинг за Linux/SSH/nginx/MySQL; geerlingguy/ansible-role-docker (*2.3k) е каноничната Docker роля. За идемпотентност прави 'ansible-playbook --check --diff' като dry-run преди реален apply — нула промени = потвърдена идемпотентност. _("IaC хардънинг + idempotency проверка за tools/vps/ansible"; verified; "https://github.com/dev-sec/ansible-collection-hardening releases/latest=10.6.0 (живо 2026-06-26); https://github.com/geerlingguy/ansible-role-docker (живо 2026-06-26)")_
- **2026-06-26:** Image scan gate с trivy: 'trivy image --exit-code 1 --severity CRITICAL,HIGH --ignore-unfixed <image>' спира деплоя при критични уязвимости — паттернът е каноничен в aquasecurity/trivy (*36.6k) и trivy-action. Мястото в autodeploy.sh е между 'docker compose build' и 'up -d', преди health check. --ignore-unfixed реже шума. _("pre-deploy image scan gate за zabobovdol Docker стек"; verified; "https://github.com/aquasecurity/trivy-action README (exit-code/severity полета, живо 2026-06-26); trivy latest v0.71.2 (2026-06-19)")_
- **2026-06-26:** fail2ban последен GitHub release е 1.1.0 (25.04.2024) — няма по-нов мажорен release към 2026-06-26. За уеб слой комбинирай с CrowdSec; не чакай свежа fail2ban версия. _("IPS/банване на SSH и уеб слой на VPS"; verified; "https://api.github.com/repos/fail2ban/fail2ban/releases/latest (живо, 2026-06-26)")_
- **2026-06-26:** Caddy текущ стабилен release е 2.11.4 (03.06.2026, не-prerelease). Референции към 'Caddy 2.9' са остарели — текущият клон е 2.11.x; ползвай него за вграден ACME + HTTP/3. _("избор на reverse proxy с вграден ACME/HTTP/3"; verified; "https://api.github.com/repos/caddyserver/caddy/releases (живо, 2026-06-26)")_
- **2026-06-26:** Docker Engine текущ е 29.6.0 (18.06.2026, серия 29.x не-LTS). Серия 28 е EOL от 13.05.2026 — хостове на 28.x да мигрират към 29.x. Допълва по-старото правило 'Docker >=29.5.1 заради docker cp TOCTOU'. _("избор/ъпдейт на Docker Engine версия на VPS"; verified; "https://endoflife.date/api/docker-engine.json (живо, 2026-06-26)")_
- **2026-06-26:** CVE-2026-55200 (critical) в libssh2 (юни 2026, + CVE-2025-15661/CVE-2026-55199 high) е в КЛИЕНТСКАТА ssh библиотека (git/curl/php-ssh), НЕ в OpenSSH sshd сървъра. Не бъркай libssh2 CVE с OpenSSH сървърен фикс. _("разграничаване на обхват при оценка на SSH рискове на хоста"; verified; "https://www.openwall.com/lists/oss-security/2026/06/ (живо, 2026-06-26)")_
- **2026-06-26:** nginx 1.31.2 и 1.30.3 (17.06.2026) поправят CVE-2026-42530 (HTTP/3 QUIC use-after-free), CVE-2026-42055 (heap overflow при ignore_invalid_headers off + големи large_client_header_buffers към HTTP/2/gRPC backend) и CVE-2026-48142 (charset_map UTF-8 heap overread). Ъпдейтни фронт-nginx до >=1.31.2 (или 1.30.3 на стария клон), особено при включен HTTP/3. _("nginx reverse proxy пред zabobovdol/medqr; Caddy не е засегнат"; verified; "https://nginx.org/en/CHANGES (живо, 2026-06-26)")_
- **2026-06-26:** Ubuntu 26.04 'Resolute Raccoon' е текущият LTS (издаден 2026-04-23, standard support до 2031-04-30, ESM до 2036). 24.04 'Noble Numbat' остава поддържан LTS до 2029-05-31 (последен point 24.04.4, 2026-02-12). За нов хост обмисли 26.04. _("избор на ОС за нов VPS; CLAUDE.md default казва 24.04 — все още валиден"; verified; "https://endoflife.date/api/ubuntu.json (живо, 2026-06-26)")_
- **2026-06-25:** Let's Encrypt вече издава 6/45-дневни сертификати — задължи ARI авто-подновяване; Docker ≥29.5.1 заради docker cp TOCTOU. _("Let's Encrypt 6/45-дневни + ARI; Docker cp TOCTOU"; verified; "https://letsencrypt.org/2026/01/15/6day-and-ip-general-availability ; https://letsencrypt.org/2025/12/02/from-90-to-45 ; Docker cp TOCTOU — сверявай Docker security advisory")_
- **2026-07-06:** panev deploy стекът е PM2 (не systemd unit) + Nginx + certbot; app слуша app.listen(PORT) на 0.0.0.0 без SIGTERM handler. UFW (22/80/443) смекчава експозицията, но за prod-коректност се добавя listen на 127.0.0.1 + server.close() на SIGTERM/SIGINT. _("panev деплой на VPS"; verified; "panev/server.js:1220 (тествано: SIGTERM → незабавен изход, без graceful) + scripts/deploy.sh:181")_
- **2026-07-06:** В .env файл, source-ван от bash (set -a; source .env под set -euo pipefail), placeholder стойност със спецсимволи като ADMIN_PASSWORD=<impostare ...> дава bash syntax error (< и > са редирекции) и аборти целия deploy. Placeholder-и в .env.example трябва да са празни или в кавички; за тайни предпочитай празно + авто-генериране в seed. _("Node/Express .env + bash deploy скриптове, които source-ват .env"; verified; "panev/.env.example:24 + scripts/deploy.sh:38,123,164 (възпроизведено: 'syntax error near unexpected token newline')")_
- **2026-07-30:** Инструмент, който чете слушащите портове САМО през `ss`, ослепява тихо на минимална машина без iproute2: връща 0 реда и панелът рисува „0 слушащи сокета" при отворени портове. Резервата е `/proc/net/{tcp,tcp6,udp,udp6}` — адресът е шестнайсетичен с ОБЪРНАТИ байтове по думи от 4 байта (`0100007F` = 127.0.0.1, `0F02000A` = 10.0.2.15; IPv6 = четири такива думи), слушащите се филтрират по `st` (`0A` = TCP LISTEN, `07` = UDP UNCONN — останалото са активни ВРЪЗКИ), а PID-ът се намира по inode: поле 10 на реда срещу връзките `/proc/<pid>/fd/*` → `socket:[<inode>]`, оттам `comm` и unit-ът от `/proc/<pid>/cgroup`. Бонус: `ss -tlnp` е само TCP → DNS/WireGuard/QUIC са невидими. _("четене на слушащи портове в контейнер/минимален образ"; verified; "живо в контейнера на сесията: `ss` липсва (run() пада) → /proc резервата даде 7 реда с истински PID/comm, потвърдено в браузър (Playwright, секция „Портове"); vps-dashboard/src/ports.js:31-151; test/ports.test.js (23/23 pass, вкл. обръщането на байтовете)")_
- **2026-07-30:** Смяната на порт през `sed` по СТОЙНОСТТА сменя и чужди редове: във файл с `TIMEOUT_MS=3000`, `PORT=3000`, `EXPORT_PORT_NOTE=3000` и коментар „порт 3000" сляпото търсене пипа четири места. Анкерът трябва да е ключът: `s/^(\s*(export\s+)?PORT\s*=\s*)["']?3000["']?\s*$/\13100/`, плюс `grep` за ПОТВЪРЖДЕНИЕ след записа (непотвърден запис = откат). _("автоматизирана редакция на .env от скрипт"; verified; "vps-dashboard/test/ports.test.js — генерираният sed се ИЗПЪЛНЯВА с bash срещу фикстурата и се сравнява ред по ред: сменя се само `PORT`, останалите три остават 3000; vps-dashboard/src/portchange.js:257-261")_

## Карантина (непроверено — не се чете като факт)
- **2026-07-27:** Обща папка за дъмпове и за архиви на Docker томове чупи и „възрастта на бекъпа“, и планираната проба за възстановяване: най-новият файл е tar.gz, който валидаторът на имена отхвърля → перманентна фалшива критична аларма. _("бекъп аларми"; high; "vps-dashboard/src/backups.js:11-19,98 + src/volumes.js:140 + src/drill.js:25,119")_
- **2026-07-27:** Възстановяване на SQLite с `cp` върху жива база без спиране на услугата, без изтриване на -wal/-shm и без chown обратно към юзъра на услугата дава „успех“, след който приложението не може да пише. Postgres restore през psql без ON_ERROR_STOP=1 излиза с код 0 при пълен провал. _("restore на бази"; high; "vps-dashboard/src/backups.js:158-190 срещу коректния модел в deploy/autodeploy.sh:244-248")_
- **2026-07-27:** Задача, пусната от systemd услуга, живее в нейния cgroup — `systemctl restart <самата услуга>` (KillMode=control-group) я убива по средата. autodeploy.sh го решава с systemd-run --on-active=5 при CSD_SELF_DEPLOY, но същият риск важи за apt/needrestart и за бутона „рестарт" върху собствения unit. _("самодеплой/саморестарт на панел-услуга"; high; "deploy/autodeploy.sh:607-627 + vps-dashboard/src/jobs.js:40 + src/services.js:63")_
- **2026-07-27:** systemd ProtectHome=true прави /root недостъпен и празен за юнита И за всички негови деца — панел с root права, чийто деплой канон живее в /root (archiveDir, ръчно качен ZIP, autodeploy find_archive), тихо вижда нула архива и не може да качва. Проверявай съвместимостта на sandbox-а с пътищата на данните преди да го обявиш за втвърдяване. _("systemd sandbox vs. пътища на данните"; high; "vps-dashboard/deploy/vps-dashboard.service:23 + src/config.js:35 + src/deploy.js:52 + src/upload.js:28 + deploy/autodeploy.sh:25,133")_
- **2026-07-16:** Папката на проекта в репото е преименувана `ospedali/` → `ospedalitrasparenti/` (по искане на собственика, при merge в main). Деплой КЛЮЧЪТ остава `ospedali` (PROJECTS, systemd услугата `ospedali`, `/opt/ospedali`, OSPEDALI_* env) — на сървъра нищо не се преименува. `deploy_ospedali` търси `$SRC/ospedalitrasparenti` с fallback към `$SRC/ospedali` (стари архиви работят). Старите бележки по-долу с път `ospedali/...` четат се като `ospedalitrasparenti/...`. _("монорепо папка ↔ деплой ключ на ospedali"; verified; "/home/user/Few-few/deploy/autodeploy.sh (deploy_ospedali fallback), .github/workflows/ospedali.yml (paths), CLAUDE.md таблица")_
- **2026-07-15:** ospedali деплоят ВИНАГИ подава IndexNow след успешен health check — `deploy_ospedali` вика `node src/indexnow.js` (best-effort; иска сайтът да е жив зад публичния домейн+TLS, за да се верифицира ключът, затова при първия деплой преди DNS/certbot може да падне и минава при следващия). Ключът е `config.indexNowKey`; `build-site.js` генерира `site/<key>.txt`; скриптът чете `site/sitemap.xml`, вади `<loc>` URL-ите и POST-ва към `api.indexnow.org`. Покрива Bing/Yandex/Seznam. **Google НЕ поддържа IndexNow** и махна sitemap „ping" → за Google откриваемостта е пасивна (sitemap.xml + robots.txt) плюс **еднократна** верификация в Search Console от собственика (DNS TXT / meta) — не се автоматизира. _("SEO push при деплой на статичен сайт; ospedali"; verified; "/home/user/Few-few/deploy/autodeploy.sh (deploy_ospedali → node src/indexnow.js); ospedali/src/indexnow.js + test/indexnow.test.js (200 теста pass, tsc strict EXIT 0); ospedali/src/build-site.js (емитва site/<key>.txt)")_

- **2026-07-15:** ospedali на живо (Hetzner, Ubuntu, Node v20.20.2) crash-loop-ва при старт със `status=31/SYS` (SIGSYS) — seccomp убива Node заради io_uring syscalls на libuv, които не са в `@system-service` на този systemd. `MemoryDenyWriteExecute=false` НЕ стига. Фикс: `SystemCallErrorNumber=EPERM` в unit-а (връща грешка вместо kill → libuv пада на epoll). Вкарано в `deploy/systemd/ospedali.service`. _("systemd hardened unit + Node ≥20 (libuv io_uring)"; verified; "живо на сървъра: journalctl status=31/SYS, dmesg празен (kernel logs заключени); фикс потвърден чрез EPERM override → node тръгва; deploy/systemd/ospedali.service")_
- **2026-07-15:** ospedali `config.js loadDotEnv()` четеше `server/.env` със `readFileSync` БЕЗ try/catch → ако `.env` е `root:root 600` (създаден със `sudo tee` без chown), www-data получава `EACCES` на `open('.env')` и сервизът пада (`status=1`), макар systemd вече да е заредил тайните през `EnvironmentFile`. Фикс: try/catch (best-effort) — тайните идват от systemd, нечетим `.env` не спира старта. _("app, което САМО чете .env, при systemd EnvironmentFile модел"; verified; "живо: journalctl EACCES open /opt/ospedali/server/.env; server/lib/config.js:39; поправено с try/catch")_
- **2026-07-15:** ospedali nginx vhost с `443 ssl` блок (референция към `/etc/letsencrypt/live/<домейн>/fullchain.pem`, който още го няма) чупи `nginx -t` ГЛОБАЛНО → блокира reload на ВСИЧКИ сайтове + `certbot --nginx` (той пуска nginx -t). Решение (потвърдено на живо): временно HTTP-only vhost (само :80) → `nginx -t && reload` → `certbot certonly --nginx -d …` издава серта → върни пълния vhost (сега серта съществува) → reload. _("първо издаване на TLS на споделен nginx хост с готов 443 vhost"; verified; "живо: nginx emerg 'cannot load certificate'; certbot 'Successfully received certificate'; после пълен vhost минава nginx -t")_

## Карантина (непроверени — НЕ са факт)
- **2026-07-14:** ospedali/server чете първата стойност на X-Forwarded-For (server.js:46) за анонимния дневен хеш И за throttle на /admin → nginx трябва да задава X-Forwarded-For $remote_addr ПРЕЗАПИС, не $proxy_add_x_forwarded_for (append би позволил подправяне на веригата). _("nginx reverse proxy пред app, който rate-лимитира по XFF"; high; "/home/user/Few-few/ospedali/server/server.js:46,68; ospedali/deploy/nginx/ospedalitrasparenti.it.conf")_
- **2026-07-14:** ospedali сервизът чете server/.state/ (config.js:28 mkdir само ако родителят е записваем) — под ProtectSystem=strict + ReadWritePaths=…/server/.state, .state/ ТРЯБВА да съществува преди старт, иначе mkdir на родителя пада. Деплоят го създава с install -d преди restart. _("systemd sandbox + приложения, които сами създават state папка"; high; "/home/user/Few-few/ospedali/server/lib/config.js:28-29; ospedali/deploy/systemd/ospedali.service (ProtectSystem=strict, ReadWritePaths)")_
- **2026-07-14:** ospedali деплой е systemd модел БЕЗ npm ci/build — нула зависимости, обслужва предбилднат site/ от git; deploy_ospedali прави само rsync (изключва server/.env, server/.state/) + systemctl restart + health /healthz + rollback. _("деплой на ospedali в монорепото"; high; "/home/user/Few-few/deploy/autodeploy.sh (deploy_ospedali); ospedali/package.json (нула deps, scripts.serve=node server/server.js)")_
- **2026-07-09:** rsync --delete НЕ трие файлове на приемника, покрити от --exclude (нужен е --delete-excluded) → изключените node_modules/dist оцеляват в deploy папката между пусканията; чисто е само ако .dockerignore в корена на build context-а ги покрива. Внимание: --exclude '.env' пази само литерала — .env.backup се трие. _("rsync-базирани деплой синхронизации с Docker build context"; unverified; "man rsync (--delete/--delete-excluded семантика); не възпроизведено на живо (rsync липсва в контейнера на сесията)")_
- **2026-07-09:** Смяна на compose project name (-p) върху СЪЩИЯ compose файл тихо изоставя данните: named volumes се префиксват с проекта (infra_aso_pgdata vs aso_aso_pgdata), новият проект създава ПРАЗНИ обеми, а старите остават осиротели (един volume prune ги затрива). Комбинирано с авто-генериран нов .env това е пълна загуба на прод база от гледна точка на потребителя. Гейт: жив legacy обем + липсващ .env → осинови стария .env или абортирай; миграция = docker run --rm -v old:/from:ro -v new:/to alpine cp -a при свален стек (оригиналът остава като бекъп). _("compose деплой при преименуване на проект / нова deploy папка"; high; "мисловна симулация ред по ред на infra/autodeploy.sh:25-106 срещу живия VPS стек (проект infra, обеми infra_aso_pgdata) + собствена verified поука 2026-07-03 за project name от директорията на compose файла")_
- **2026-07-09:** Vizitka .pkpass подписът spawn-ва системния openssl (execFileSync smime detached PKCS#7); hardened systemd unit-ът го позволява: ProtectSystem=strict държи /etc четим за сертификатите, PrivateTmp дава изолиран /tmp за os.tmpdir(), SystemCallFilter=@system-service разрешава fork/exec. Съвместимо — не разхлабвай sandbox-а заради портфейлите. _("systemd sandbox + приложение, което spawn-ва openssl за подпис"; high; "/home/user/Few-few/vizitka/src/wallet/apple.js:107,132 ; /home/user/Few-few/vizitka/deploy/systemd/vizitka.service:22,20,39")_
- **2026-07-09:** Ubuntu 24.04 = OpenSSL 3.x; legacy provider не е зареден по подразбиране, затова 'openssl pkcs12 -in' върху Keychain-експортиран .p12 (PBE-SHA1-3DES/RC2) фейлва с 'unsupported'. Нужен е флаг -legacy за извличане на Apple Pass PEM-ите. Runtime 'openssl smime' подписът не е засегнат. _("Apple Wallet .pkpass сертификати на Ubuntu 24.04"; medium; "/home/user/Few-few/vizitka/deploy/DEPLOY.md §7 ; OpenSSL 3 legacy provider behavior; зависи от начина на експорт на .p12")_
- **2026-07-09:** backup.sh, който върви като непривилегирован потребител през cron и прави mkdir -p /var/backups/<app>, фейлва защото /var/backups е root:root 755. Bootstrap-ът трябва да пре-създаде директорията: install -d -o app -g app -m 700 /var/backups/<app>. _("cron бекъп като системен потребител"; high; "/home/user/Few-few/vizitka/deploy/backup.sh:15,20 ; /home/user/Few-few/vizitka/deploy/server-setup.sh:161-163")_
- **2026-07-09:** nginx vhost с 443 блок, чиито ssl_certificate пътища още не съществуват, кара 'nginx -t' да фейлва глобално; затова 'certbot certonly --nginx' на чист хост прекъсва (плъгинът пуска configtest). Издавай сертификата с HTTP-only vhost (или --webroot) първо, после активирай 443. 'certonly' никога не редактира vhost-а. _("certbot + nginx първо издаване в автоматизиран bootstrap"; high; "/home/user/Few-few/vizitka/deploy/server-setup.sh:130-140 ; /home/user/Few-few/vizitka/deploy/nginx/vizitka.conf:18-19")_
- **2026-07-09:** age-keygen -o FILE пише 'Public key: age1...' на stderr (без '# '), докато '# public key:' форматът е само ВЪТРЕ в ключовия файл. Скрипт, който парсва recipient със sed '^# public key:' от stderr, получава празно и тихо пропуска бекъпа. Устойчиво: AGE_RECIPIENT=$(age-keygen -y KEYFILE). _("age-базирани криптирани бекъпи в bootstrap скриптове"; high; "/home/user/Few-few/vizitka/deploy/server-setup.sh:150-151 (code review); age-keygen documented output format")_
- **2026-07-07:** systemd 255 (Ubuntu 24.04) проваля unit с 226/NAMESPACE ако ReadWritePaths сочи несъществуващ път; префикс '-' го прави толерантен. Nginx add_header за Cache-Control върху /_next/static/ дублира хедъра, който Next вече праща (immutable). _("systemd sandbox + nginx proxy на Next"; high; "https://www.freedesktop.org/software/systemd/man/systemd.exec.html#ReadWritePaths=")_
- **2026-07-07:** Реда npm ci → next build → npm prune --omit=dev е задължителен: build иска typescript/tailwind/postcss (devDeps), а рънтаймът не ги иска САМО ако next config е .mjs/.js (next.config.ts зарежда typescript при старт → гърми след prune). _("Next.js production билд на VPS"; high; "/home/user/Few-few/mastilko/next.config.mjs:1-2; /home/user/Few-few/deploy/autodeploy.sh:286-289")_
- **2026-07-07:** Next.js 15 systemd деплой с ProtectSystem=strict работи с ReadWritePaths=<app>/.next/cache само ако е БЕЗ output:'standalone' (иначе ExecStart е node .next/standalone/server.js). При next/image употреба .next/cache/images прави записа НУЖЕН, не просто адекватен; sharp е optionalDependency на next и оцелява npm prune --omit=dev. _("Next.js systemd деплой зад nginx"; high; "/home/user/Few-few/mastilko/next.config.mjs; /home/user/Few-few/mastilko/deploy/mastilko.service; /home/user/Few-few/mastilko/src/components/Logo.tsx")_
- **2026-07-06:** Nginx rate limiting ползва leaky-bucket чрез limit_req_zone/limit_req с burst= и nodelay; зад прокси/CDN трябва realip за да лимитираш реалния клиент, не прокси IP. Точните параметри и текст на официалния блог НЕ са потвърдени дословно (страницата не се извлече чисто). _("edge rate limiting като трети слой над CrowdSec/fail2ban"; unverified; "https://blog.nginx.org/blog/rate-limiting-nginx (не извлечен чисто)")_
- **2026-07-02:** Ред в .dockerignore, който изключва цяла директория (напр. `deploy`), кара всеки `COPY deploy/...` в Dockerfile да се провали с 'no source files' — build блокер. Провери съответствие .dockerignore ↔ COPY преди деплой. _("docker build; multi-stage Dockerfile с COPY на deploy артефакти"; unverified; "/home/user/Few-few/Nexus/.dockerignore + /home/user/Few-few/Nexus/Dockerfile:33")_
- **2026-06-30:** supreme изисква ЧЕТИРИ env файла за carry-over, не два: корен supreme/.env (POSTGRES_* за compose интерполация), backend/.env, bot/.env и frontend/.env. frontend/.env е build-time — Vite го чете при `npm run build` в Dockerfile (COPY . . → build), затова трябва да е на място ПРЕДИ docker compose build, иначе VITE_* стойностите се пропускат тихо. _("supreme env carry-over"; high; "file:/home/user/Few-few/supreme/frontend/Dockerfile, file:/home/user/Few-few/supreme/frontend/.env.example, file:/home/user/Few-few/supreme/.env.example")_
- **2026-06-30:** Supreme Bot (supreme/) деплой моделът е Docker Compose; собственият supreme/deploy.sh прави up -d --build, чака botpanel_backend да стане healthy (миграциите се пускат от backend/docker-entrypoint.sh чрез `npx prisma migrate deploy`), после регистрира slash командите. Единственият публичен порт е frontend nginx на 127.0.0.1:8080. _("supreme deploy"; high; "file:/home/user/Few-few/supreme/deploy.sh, file:/home/user/Few-few/supreme/backend/docker-entrypoint.sh, file:/home/user/Few-few/supreme/docker-compose.yml")_
- **2026-06-26:** OpenSSH releasenotes (openssh.com/releasenotes.html) не върна съдържание през агентското прокси на 2026-06-26 — текущата OpenSSH версия НЕ е потвърдена този ден; остава 'за проверка', не приемай за факт. _("оценка на OpenSSH версия/CVE на хоста"; unverified; "openssh.com/releasenotes.html върна празно през прокси (живо опит 2026-06-26)")_
