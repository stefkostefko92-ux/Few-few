import type { Metadata } from "next";
import { buildMetadata, webPageLd } from "@/lib/seo";
import { SITE } from "@/lib/site";
import { PageHero, Prose } from "@/components/ui";
import { JsonLd } from "@/components/JsonLd";
import { markdownToHtml } from "@/lib/markdown";

export const metadata: Metadata = buildMetadata({
  title: "Достъпност",
  description:
    "Декларация за достъпност на сайта на ФК „Миньор“ Бобов дол и наличните помощни настройки.",
  path: "/dostapnost",
});

const CONTENT = `
Стремим се сайтът на ФК „Миньор“ Бобов дол да е достъпен за всички, включително
за хора с увреждания, в съответствие с европейския стандарт **EN 301 549**
(прилагащ **WCAG 2.1, ниво AA**) и приложимото национално законодателство.

## Помощни настройки

В горната лента за достъпност можете да:

- **Уголемите текста** на три нива, без да се нарушава подредбата.
- Включите **висок контраст** за по-добра четимост.
- Включите **тъмен режим**, по-щадящ за очите на тъмно.
- Включите **по-лесно докосване** — по-големи бутони и връзки.

Изборите ви се запомнят в браузъра при следващото посещение.

## Заложени мерки за достъпност

- Семантичен HTML и логична структура на заглавията.
- Връзка „Прескочи към съдържанието“ за навигация с клавиатура.
- Видим фокус на елементите и достатъчен цветови контраст.
- Текстови алтернативи на изображенията и описателни връзки.
- Зачитане на системната настройка „по-малко движение“ (reduced motion).

## Състояние на съответствието

Сайтът е в **съществено съответствие** с EN 301 549 / WCAG 2.1 ниво AA.
Самооценка (вътрешен преглед на кода и страниците), последно прегледана на
**01.06.2026 г.**. Известни ограничения: част от снимките (състав, галерия) се
подават от външни източници и качеството на алтернативния текст зависи от
въведеното от редактора; стремим се към пълно покритие.

## Обратна връзка и ескалация

Ако срещнете затруднение с достъпността на сайта или имате нужда от
информацията в друг формат, пишете ни на **${SITE.contact.email}** или се
обадете на **${SITE.contact.phone}**. Ще се постараем да помогнем възможно
най-бързо.

Ако не получите задоволителен отговор в разумен срок, можете да подадете сигнал
до съответния надзорен орган по достъпността в Република България.
`;

export default function AccessibilityPage() {
  return (
    <>
      <PageHero
        title="Достъпност"
        eyebrow="За всички"
        crumbs={[{ name: "Достъпност", path: "/dostapnost" }]}
      />
      <JsonLd
        data={webPageLd({
          name: "Декларация за достъпност",
          path: "/dostapnost",
          lastReviewed: "2026-06-01",
        })}
      />
      <div className="container-content max-w-3xl py-10">
        <Prose html={markdownToHtml(CONTENT)} />
      </div>
    </>
  );
}
